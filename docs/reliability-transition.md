# Переход к надёжному iPad

Эта карта различает реализованное поведение и ещё не завершённый переход из
[audit-2026-09-08.md](audit-2026-09-08.md). Текущий код читает только новый формат.
Амир отменил восстановление старых архивов и подтвердил, что копии остаются
в резерве. Новое текущее пространство не заменяется и не объединяется с ними.
11 сентября, 17:44 МСК, на iPad подтверждён только новый bundle
`com.amirtlinov.notebook.preview`, 0.3.16 (19). Mac имеет ту же версию.
Установленный срез чата и основной код — разные наборы; их объединение
подготовлено отдельно и пока не прошло общий маршрут. Точные результаты
публикуются в [verification.md](verification.md).

Последний полный неизменный срез завершён 11 сентября 2026, 15:32 МСК:
530 Core, 26 Codex, 26 external, 44 MCP, 39 проверок первого установщика,
40 сборщика пары, 113 Mac и 483 iPad, без ошибок, пропусков и runtime warnings.
Квитанция связывает 522 исходных файла и 1879 файлов свидетельств.
Один источник готовит физические DOM-фрагменты на уже выделенной поверхности;
соседние WebKit не повторяют его разметку и MathJax. Передача учитывает размер
фактического пакета, а разметка освобождается со своим последним владельцем.
Новая область доверия назначается проверенной активацией до запуска модели.
Точные свидетельства — в [verification.md](verification.md), контракт разметки —
в [document-page-fragments.md](document-page-fragments.md).

Независимые программы в пассивных iframe, остальные условия ниже и физический
показ большого документа пока открыты. Единый путь сохранения листа и отказ конфликтующего штриха
прошли этот полный срез;
[контракт и профили](page-ink-conflict-contract.md).

Нативный адрес продолжения теперь включает смещение внутри исходного блока,
в тех же физических единицах, что бумажная рамка. Профиль 118 Mac / 48 iPad
прошёл на неизменных исходниках; полный новый маршрут ещё впереди.
Пассивная страница по-прежнему запускает отдельную программу: это подтверждённый
отрицательный сценарий, а не закрытое условие W4.
[Контракт и свидетельства](document-program-cuts.md).

## Реализованный путь

- Сборщик пары использует ту же полную опись исходников и проверку iPad-подписи,
  что полный маршрут и установщик первого Lab. MCP и сам `verify.sh` больше не
  выпадают из описи установщика. Новая квитанция связывает полный проход с обоими
  xcresult; отдельная команда готовит подписанную пару, но не имеет операции
  установки или изменения архива. 79 искусственных CLI-проверок прошли;
  полный `d4b4b40` получил настоящую квитанцию маршрута; подписанная пара чата 0.3.16 проверена отдельно от нынешнего основного кода. [Контракт](release-build-contract.md).
- Внешний `notebook-archive-transfer` переводит независимую файловую копию в
  полный новый архив. `NotebookCheckpoint` проверяет владельцев и зависимости;
  `NotebookStore` публикует весь результат одной транзакцией и не заменяет
  существующее назначение. Исходные хеши, точный readback и перечень преобразований
  входят в отчёт. Добавочный режим переносит старых независимых владельцев в
  полную копию нового Lab через обычную публикацию; прежние запросы, остановки,
  источники и присутствие Lab сохраняют значения и хеши. Подготовку пары и атомарную активацию до запуска модели исполняет общий
  bootstrap. После копирования bootstrap принудительно сохраняет оба архива
  и манифест на устройстве назначения до обмена; восстановление без квитанции
  повторяет эту границу, но не откатывает архив. 15 Core и 26 external проверок
  этой границы прошли, включая ошибки записи и допуска; это не физическое
  отключение питания. Старые декодеры доски и документа теперь находятся только во внешнем конвертере; его полный readback включает явные причинные часы того же владельца (`docs/retired-owner-conversion.md`). Восстановление реальных старых архивов отменено Амиром. Копии остаются в резерве;
  отменённый перенос не считается выполненным, а копия не доказывает исторический учёт UUID;
  [контракт](archive-transfer.md).
- `NotebookStore` публикует адресное содержание, причинную версию, квитанцию и
  журнал доставки в одной транзакции SQLite WAL. `CollaborationStore` проверяет
  ожидания и отмену; у нативного ввода, MCP и сети нет второго писателя.
- `NotebookAppModel` закрепляет страницу при начале Pencil и синхронно принимает
  UUID и точки при отпускании. Одна очередь готовит чернила вне главного актора,
  удерживает ошибку и зависимые действия при смене или вытеснении страницы.
  Распознанная отмена входит в тот же порядок. Shutdown закрывает новые контакты
  до ожидания и подтверждается только после сохранения уже принятого ввода.
- UI и MCP читают тетрадь через `NotebookPageWindow`, `NotebookPagePosition` и
  заголовки. Модель удерживает до четырёх тел, а номер связывает с UUID и
  неизменяемым порядком. Обновление сцены не вытесняет подготовленную дальнюю
  цель; поздний callback прежнего порядка не выбирает новый лист на том же месте.
  Конкурентная посадка сохраняет принятый UUID и уточняет его фактическую позицию.
  Удаление перечисляет собственные листы и адресные поддеревья порциями по 64.
  `notebook_read_notebook` читает метаданные без тел; продолжение проверяет корень.
  Частичная проекция больше не предоставляет канонический индекс страницы.
- `SceneCompositionTiles` публикует целую композицию. Родитель и портал передают
  тот же `InkCanvasView`; новый контакт удерживает показанную матрицу. Бюджет
  256 МиБ защищает половину для ввода. При давлении сначала укрупняется полный
  фоновый обзор, затем исключаются необязательные живые элементы; допущенный
  портал и закреплённые владельцы сохраняются.
- Кешируемые SwiftUI-описания не получают аренду растров в конструкторе.
  Установленные нативные поверхности держат собственные аренды; временная
  передача сохраняет пиксели, действительное снятие освобождает их. Устойчивый
  shutdown напрямую завершает зарегистрированные поверхности, даже если UIKit
  удержал снятый view; чужая самостоятельная аренда не отзывается.
- `NotebookPreparationWindow` даёт рендеру оконное окружение, но не может стать
  ключевым окном, принимать ввод или появляться в дереве доступности. Камера
  сохраняет базу подтверждённой пары пальцев; признание pinch не обнуляет движение.
- `DocumentRenderSession` разделяет неизменяемое содержание, `DocumentStateJournal`
  изменяет состояние без повторной загрузки HTML. Эпоха редактора отделена от
  канонического показа; миниатюра заимствует пиксели у настоящего WebKit-владельца.
  `UIPageViewController.pageCurl` удерживает не больше четырёх живых содержаний.
- Указание привязано к показанному физическому источнику и перепроверяется с SQL.
  `SharedContext` хранит указания и совместные ходы; каноническая переписка
  принадлежит Codex. Адреса, версии и причинную отмену проверяет `CollaborationStore`.
  Карточка и человеческая рамка принадлежат одному `NotebookAgentQuestion`:
  закрытие снимает активный выбор устойчиво, не удаляя историю или запрос.
  Поздняя запись и перезапуск не возвращают рамку; продолжение из истории явно
  восстанавливает исходное указание. Это проверено native-тестами и пикселями UI.
  `NotebookCodexSidecar` передаёт сообщения установленному Codex, а не запускает
  модель. Устойчивая очередь сохраняет попытку до отправки, сверяет неоднозначное
  принятие и не прерывает занятую задачу. Прежний исполнитель, отдельный вход
  и разделение вопрос/изменение удалены; архивные запросы только читаются.
  Сохранение содержания и получение его iPad подтверждаются прежними квитанциями.
- Mac имеет только `MenuBarExtra`, IPC и подготовку PNG. Транспорт использует
  подтверждённые идентичности, TLS, кадры до 256 КиБ и окно из 16 кадров;
  дисковый кредит не заменяет подтверждение устойчивого применения.

## Обнаруженный дефект показа

MCP подтвердил связь и чтение текущего документа, но снимок первого листа
возвращает ошибку. Пустая страница на сохранённом кадре iPad не является
доказательством утраты содержания. [Живая проверка](live-document-render-boundary.md)
фиксирует сценарий, который ещё нужно воспроизвести синтетически и исправить.

## Незавершённые условия полной реализации

Ни одно из условий ниже не закрывается успешным тестом соседнего механизма.
Ограничение размера ответа также не доказывает ограниченное внутреннее чтение.

| Владелец | Что ещё нужно реализовать | Проверяемое завершение |
|---|---|---|
| `NotebookReplicationStore`, журнал транзакции | Независимые владельцы входящего пакета теперь сливаются по одному; manifest и архивные запросы читаются порциями до 64, 20 Core-тестов прошли (`docs/replication-owner-window.md`). Адресовать слияние внутри одного тяжёлого дерева, каталога и листа. Исходник документа объединяется по одной программе и семи причинным полям, без соседних исходников и словаря истории; профиль 142 Core, 17 external, 44 MCP прошёл (`docs/document-source-replication.md`). Полное объявление программы принадлежит manifest 3, внешняя подготовка обоих устройств пересоздаёт доставку без изменения хешей содержания. Это не общая политика хранения журнала. Журнал состояния адресован по block ID; нативная очередь также передаёт один принятый блок, не весь журнал (`docs/native-document-state-command.md`). 100 000 состояний, поздняя доставка, повреждения и сбои проверены (`docs/document-state-replication.md`). Пространственная доставка уже обходится страницами по 64 адреса и использует прежний UUID-публикатор; профиль среди 100 000 действий, старое эхо и сбои прошли, `docs/spatial-replication-contract.md`. Накопление изменений и затронутых владельцев перенесено в временные SQL-таблицы с кешем 2 МиБ; manifest публикуется порциями до 16 384 записей. Ограничение срока хранения устойчивого журнала ещё открыто. | Маленькое изменение среди 100 000 владельцев не декодирует посторонние значения; большое удаление не собирает весь список изменений в памяти; rollback, повтор, старое эхо и курсор остаются в общей транзакции. |
| `SharedContext` | Добавление, прямой родитель ответа, каталог, страницы и входящее слияние адресованы. Причинный SQL-порядок не перенумеровывает старые строки; ссылки проверяются по индексу, до 512 уникальных ссылок и 4 МиБ исходников на команду. Архивный исполнитель удалён; новая переписка принадлежит Codex. Контракт — `docs/shared-context-contract.md`. | Append среди 100 000 записей не декодирует посторонние тела; страницы ограничены 64 записями и 4 МиБ, UI сохраняет одну страницу. Курсор изменяет собственная история, а не камера или выбор. Отказ слияния не меняет принятые записи. |
| `NotebookActionProjection`, `CollaborationStore` | Пространственные append/undo читают UUID операций и квитанции, не все действия поверхности; профиль среди 100 000 действий на той же доске прошёл. Состояние документа и его отмена также адресованы по blockID, профиль среди 100 000 исторических записей прошёл (`docs/agent-document-state-contract.md`). Адресная правка существующего исходника уже прошла общий маршрут `9e1b252`. Человеческое сохранение одного блока с черновиком прошло полный маршрут 11 сентября в 03:41 МСК (`docs/native-document-source-command.md`). Адресовать тяжёлые страницы, вставку/удаление/порядок блоков документа, защиту доработки при отмене создания и причинные проекции предков; `docs/agent-spatial-command-contract.md`. | Apply/undo не читает постороннее содержание, сохраняет человеческую доработку и проверяет адреса и версии. |
| `DocumentRenderSession`, `DocumentWebCoordinator` | Общая браузерная пагинация и физические DOM-фрагменты прошли полный маршрут 11 сентября, 11:36 МСК. Завершить одного активного исполнителя программы вместо независимых циклов пассивных iframe. | Большой Markdown и интерактивный блок до 2048 points, пересекающий листы: разные реальные пиксели и доступный текст, один источник состояния, без размножения DOM. |
| `SceneRenderResources`, `InkCanvasView` | Near-full перекладка реализована через переиспользование фиксированных Metal-пулов; новая полноразмерная копия не выделяется. Подтвердить глобальную стоимость CPU-подготовки и фактических ресурсов устройства. | Десять перекладок при заполненном бюджете, отмена, рост с отказом, первый контакт 13-inch Retina и пиксели на швах проверены native-тестами. Физические CPU/GPU, память и системные представления кадров ещё не измерены; CADisplayLink их не заменяет. |
| Мировые координаты | Сохраняемые адреса Swift/MCP ограничены точным диапазоном ±(2^53−1); Pencil, pan/pinch, создание, перенос и пружина допускают их до назначения. Производные края принадлежат `WorkspaceSpatialBounds`: точные строковые тайлы сохраняют внешнюю геометрию, запрос задаёт физический якорь и конечную рамку. Внешний веер остаётся в индексе и рендере, но не становится центром открытой страницы. Объединённый профиль: 76 Core, 43 MCP, 54 native; RGBA-рендер совпал с контролем во всех четырёх крайних углах, `docs/world-address-contract.md`. | Профильные адреса, JSONValue/TypeScript, SQL-курсор и пиксельные отказы проверены. Общий `d4b4b40` прошёл; физическая приёмка ещё нужна; произвольный Int64 не объявлен адресом предмета. |
| `NotebookCodexSidecar` и физическая пара | Проверить автоматическую регистрацию Notebook в существующей задаче, открытие без фокуса, сопряжённую пару и совместную работу на устройстве. Вход принадлежит самому Codex. | Реальный native-каталог, ответ, отказ в разрешении и остановка уже проверены отдельно. Полный урок с рисунком, человеческой доработкой и отменой, десять повторов и 30 минут ещё требуют физической приёмки. |

`NotebookPlacementRead` уже ограничивает весь расчёт: 4 096 препятствий,
16 МиБ исходника, 500 000 SQL-инструкций и 4 млн сравнений. `placement_budget`
означает отказ целого пакета, не пропуск препятствий. Крупное содержание одного
листа пока читается целиком под этим ограничением. Полный снимок архива остаётся
формой переноса и тестовой фикстурой, не запасным путём размещения.

Адресная посадка нового листа уже объединяет UUID, членство, первый штрих,
причинный порядок и присутствие. Неизменяемый 32-ветвевой вектор сохраняет
авторские предложения отдельно от результата слияния. UI уже использует этот
корень для адресного окна; полное чтение входящего каталога остаётся открытым.

## Связь с 26 находками

Таблица связывает исходные находки с владельцем и исполняемой проверкой.
«Срез» означает реализованный локальный контракт, а не разрешение установки;
актуальная общая проверка находится в [verification.md](verification.md).

| Находка | Механизм и исполняемая проверка | Граница |
|---|---|---|
| S1 | Причинный состав и явное удаление в SQL; выбор имеет одного владельца `SessionPresence`; `WorkspacePublicationTests`, `NotebookSQLiteTests` | Интеграция |
| S2 | Нативные изменения публикуют разницу адресной проекции и сохраняют невидимых владельцев; UI и MCP читают ограниченное окно; `NotebookSQLiteTests`, `NotebookPersistenceTests`, `NotebookPageAddressTests` | Посадка и выбор адресованы; большой журнал транзакции остаётся в условии S8 |
| S3 | Единственная транзакция SQLite WAL публикует содержание, версии, квитанцию и журнал; `NotebookSQLiteTests` проверяет rollback и неоднозначный ответ после commit | Интеграция; двойного файлового писателя нет |
| S4 | Создание предшествует первому штриху, ошибка удерживает очередь; `NotebookPersistenceTests` | Срез |
| S5 | Содержание, защита от повторов и входящий курсор публикуются вместе; `NotebookReplicationTests`, `AgentStateTests`, `MacModelLifecycleTests` | Интеграция; восстановление полных логических владельцев ещё осталось |
| S6 | Явное двустороннее сопряжение, UUID пространства и системный TLS; `NotebookTransportSessionTests`, `NearbySyncTests` | Интеграция; физическая пара ещё не принята |
| S7 | Дубликаты ID и неоднозначное физическое владение отклоняются; `WorkspacePublicationTests` | Срез |
| S8 | Ограниченные кадры и кредиты, дисковая сборка, страницы журнала и ACK после commit; `NearbySyncTests`, `NotebookTransportSessionTests`, `NotebookReplicationTests` | Интеграция; работа большого входящего пакета и срок хранения журнала ещё не ограничены |
| S9 | Контакт и камера принадлежат поколению соединения; поздний disconnect/ответ не восстанавливает старый ввод; `NotebookPersistenceTests`, `NotebookTransportSessionTests` | Интеграция; запись локального положения не входит в устойчивую репликацию |
| R1 | Пустой Metal-владелец готов без drawable/MSAA; `DrawingOwnershipTests` | Допуск native backing и завершение GPU/CA проверяются; физическая память ещё не принята |
| R2 | Регистрация и готовый mesh сохраняются при передаче; `SpatialInkProjectionTests`, `PortalPassageTests` | Срез |
| R3 | Учитываемые GPU-буферы и композиция SQL-порций в тайлы; `SpatialInkProjectionTests`, `SceneRenderResourcesTests`, `SceneCompositionSQLTests` | Подготовка и растр имеют общий допуск; физические CPU/GPU и RSS ещё не приняты |
| R4 | `WorkspaceItemPose` удерживает целую обложку и причинную посадку; Pencil фиксирует показанную матрицу и тот же InkCanvas; `WorkspaceItemPoseTests`, `WorkspaceCoverContinuityTests`, `SceneItemOwnerRetirementTests`, `SpatialInkTilePoolTests` | Контакт, перенос и near-full перекладка исполняются; физическая приёмка открыта |
| R5 | Отмена pan сохраняет показанное положение; `NotebookInputTests` | Срез |
| R6 | Pan уступает контролам и редактированию; `NotebookInputTests` | Полное согласование всех владельцев жестов открыто |
| R7 | Отмена распространяется на подготовку, readback и публикацию; `DrawingOwnershipTests` | Уже отправленный GPU завершает владение ресурсами |
| R8 | Контакт фиксирует геометрию и маршрутизирует каждый сегмент один раз; `NotebookInputTests` | Срез |
| R9 | Совпавшие точки и коррекции меняют только хвост; `IncrementalInkMeshTests` | Срез |
| W1 | Состояние и соседний текст не пересоздают iframe; `DocumentRuntimeTests` | Срез |
| W2 | Устойчивый черновик, один активный редактор и source-CAS; `DocumentEditingSessionTests`, `DocumentRuntimeTests` | Конфликт сохраняет черновик |
| W3 | Состояние и перенос не перезагружают программу; `DocumentRuntimeTests`, `AgentWebLeaseTests` | Срез |
| W4 | Только программы своего листа; миниатюра заимствует реальные пиксели; `DocumentRuntimeTests` | Общая пагинация проверена; независимые циклы пассивных программ ещё не устранены |
| W5 | Разметка без зависимости от скрытого rAF; `DocumentRuntimeTests`, проверка Mac-помощника | Неизвестное завершение программы не выдаётся за готовность |
| W6 | Эпоха редактора отзывает live receipt; снимок проверяет две квитанции и удерживает бюджет до callback; `DocumentEditorPresentationTests`, `DocumentEditorDisplayReceiptTests` | Mac и iPad Simulator проверены; физическая приёмка ещё нужна |
| Q1 | Запуск использует заголовок, окно сцены и ограниченное число тяжёлых владельцев; `NotebookSQLiteTests`, `WorkspaceSceneIndexTests` | Интеграция; растущее содержимое одного владельца и физический холодный запуск ещё открыты |
| Q2 | UI-тест касается видимого текста текущей страницы без повторов | Системные метрики и 30 минут на физическом iPad открыты |


## Живые данные Mac перед окончательным переносом

Уже работающий Mac получил новое содержание после отдельной активации. Его
1179 канонических записей и 789 файлов сохранённого прежнего архива получили
независимые копии и проверку хешей 11 сентября, 15:45 МСК. Это не окончательная
пауза рисования. Исторический Mac-источник и нынешнее назначение теперь нужно
проверять раздельно; старый отпечаток не разрешает заменить новые записи.
Подробности и сохранённые свидетельства — [current-mac-preservation.md](current-mac-preservation.md).

## Запрет преждевременной установки

Живая проверка 10 сентября подтверждает разрыв поставки: MCP текущей ветки
ожидает IPC-сокет, а установленный Mac 0.3.14 (17) его не предоставляет.
`ipc_unavailable` при существующем процессе не лечится его перезапуском.
Точная идентичность помощника и изолированные проверки приведены в
`docs/verification.md`; восстановление рабочего доступа входит в замену пары,
а не в возврат прежнего писателя файлов.

Прежний `Applications/install.sh` удалён. `Applications/install-preview.sh`
разрешает только первую установку отдельного Lab и отказывает при существующем
`com.amirtlinov.notebook.preview`; эта граница не является обновлением пары.
В установленном Lab уже есть человеческий ввод. Пустая SQL-очередь, пауза
процесса и равенство максимальных часов старой сборки не доказывают наличие всех
принятых UUID: прежние подготовки могли принадлежать снятым страницам.

Перед заменой обязательны все условия:

1. Полный `verify.sh` на неизменных исходниках, без исключений и маскирующих повторов.
2. Завершённые адресные, документные и ресурсные границы выше.
3. Для нового ввода — устойчивое сохранение; для исторического ввода прежней сборки — согласованные свежие независимые копии всех трёх архивов, проверка обнаруженных данных и визуальная сверка Амира. Не выдавать этот критерий за отсутствующий программный учёт исторических UUID.
4. Проверенный checkpoint и атомарная активация с проверкой отказов.
5. Нагрузки 1k/10k/100k уникальных владельцев и фактический архив; рабочий набор
   и работа кадра не растут пропорционально архиву.
6. Холодный запуск и физическая приёмка: системные представления кадров,
   CPU/GPU, память, десять повторов маршрута и 30 минут совместной работы.

MCP-проверки запускаются только с изолированными `NOTEBOOK_HOME` и IPC-процессом.
Собранный helper не запускается над прежним архивом. Текущее состояние
установленных приложений нельзя выводить из исторической квитанции или снимка
Simulator; настоящий архив этим срезом не преобразовывался.
