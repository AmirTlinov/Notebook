# Переход к надёжному iPad

Это карта исполнения плана из `docs/audit-2026-09-08.md`, а не разрешение
заменить установленную пару. Исходные данные устройств не конвертировались;
SQLite, доверенное сопряжение, композиция тайлов и исполнитель вопросов прошли
полный локальный маршрут; новая пара ещё не принята.
Этот промежуточный код не предназначен для смешанной пары со старым приложением:
новые причинные поля ещё не имеют завершённого протокола переноса и доставки.
Проверки MCP этой ветки используют отдельное временное хранилище и приватный
`NOTEBOOK_SOCKET` тестового процесса. Производственный `MCP/run.sh` обращается
к установленному Mac-помощнику и больше не собирает собственного писателя Core.
Тестовый сокет не является разрешением заменить настоящее хранилище.

## Проверенный промежуточный срез — 9 сентября

Полный `verify.sh` прошёл 9 сентября 2026 в 13:33 МСК: **246 Core**, **35 MCP**,
девять smoke-групп, восемь проверок настоящего App Server и шесть проверок
исполнителя с SQL, **76 Mac**, **267 iPad unit и все 38 UI-сценариев**.
Нет ошибок, пропусков и предупреждений исполнения. [Квитанция проверки](verification.md)
связывает этот прогон с неизменными исходниками и сохранёнными доказательствами
в `.build/verification-sql-20260909.4woaRU/`; это не физическая приёмка.

`NotebookPersistenceQueue` сохраняет порядок принятого ввода и удерживает
неуспешные записи с зависимостями. Исправлена ошибочная трактовка сортировки
рабочего набора как перестановки каталога: вложенное создание проходит независимо
от UUID, а настоящая перестановка по-прежнему проверяет исходное состояние.
Жесты доски не принимают касания отдельного системного меню. Публикация новых
пикселей теперь учитывает также набор физических владельцев, а не только SQL-курсор.
UI-проверки подтверждают два уровня досок, обратный проход портала, двойное
касание редактора и отмену, сохраняющую человеческое перемещение и рукопись.

Mac работает через `MenuBarExtra` без рабочего окна. Полный маршрут получил
настоящий PNG документа; отдельные тесты проверили отказ завершения с несохранённым
вводом и успешный выход после ремонта. Реальная учётная запись ChatGPT, перенос
архива и установленная согласованная пара ещё не проверены.

## Интеграция 9 сентября — не разрешение на установку

В текущих исходниках `NotebookStore` — единственный транзакционный владелец SQLite WAL;
тяжёлые неизменяемые значения адресуются хешами. `NotebookInk/2` сохраняет штрихи
листа по UUID, данные точек и импортированный растр — отдельными значениями.
Повтор штриха и отмена проверены без повторной публикации его точек.
На Mac прежние файловое наблюдение и запись MCP удалены: команды проходят локальный
сокет одного процесса. Камера и выбранная страница сохраняются через `SessionPresence`.

SQLScale с 100 000 уникальных предметов в последнем полном Debug-прогоне
входит в **246/246** Core; весь набор занял **140,283 с**. Отдельный прежний
профиль той же реализации создания и адресного изменения показал максимальный
RSS около **912 МиБ**; логи и три профиля сохранены в `.build/sql-scale-2026-09-09/`.
Эти числа включают построение архива и не измеряют холодный запуск, отдельную
задержку чтения или память обычного рабочего набора. Плавность этим не принята.
[Контракт транспорта](transport-contract.md) фиксирует реальные TLS-проверки,
дисковую сборку, ограничение кадров и подтверждение после сохранения.

Карточка вопроса проверена в общем UI-маршруте: выделение не открывает
клавиатуру само, вопрос сохраняется без связи. Проверки настоящего App Server
используют локальный mock-provider, а не вход пользователя ChatGPT.

Mac-интеграция отдельно воспроизвела ошибку сборки входящего старого пакета
с более новым локальным узлом: прежние общие часы не могли описывать новый
состав. Регрессионный Core-тест проверяет сохранение локального перемещения,
камеры портала и нового удалённого документа. Ответ с составным эмодзи выявил
сравнение Swift-символов вместо точных UTF-8-байтов; поток теперь удерживает
одну неизменяемую порцию до устойчивого подтверждения. Повторная проверка
реального SQL → App Server с локальным mock-provider проходит шесть сценариев.
Это не настоящий ChatGPT-вход и не доказательство работы установленного iPad.

Прежний `Applications/install.sh` удалён. Он заменял программу iPad до
подтверждения сохранения принятого ввода и не проверял новый закрытый checkpoint.
До реализации безопасного маршрута из раздела переноса у ветки нет команды
установки поверх настоящих данных. Установленные приложения не изменялись.

`AgentRequest` связывает текст в `SharedContext`, неизменяемое разрешение и
закреплённые исходники. Исполнение, порции ответа, запрос остановки и квитанции
имеют устойчивые отдельные адреса. Только приватное разрешение исполнителя
допускает изменение внутри выделения; повтор вызова получает прежнюю квитанцию,
а подтверждённая остановка запрещает поздние действия. Карточка iPad и Mac-владелец
исполнения подключены и прошли полный локальный маршрут. [Контракт исполнителя](agent-runtime-contract.md)
отделяет тесты настоящего App Server с локальным поставщиком ответов от ещё не
проверенного пользовательского входа ChatGPT. У выделения без сохранённого
исторического кадра нельзя объявлять визуальное рассмотрение выполненным.

Адресный поиск FTS готов: `NotebookSearchIndexTests` проверяет названия и текст,
русский язык, регистр, диакритику и подстроки длиной один-два символа. Идентичность
полного физического владельца берётся из SQL вместе с показанной проекцией;
`NotebookReferenceIndexTests` проверяет независимость посторонних владельцев,
порядок элементов и порционное чтение регионального доказательства. Финальная
публикация региона сверяет исходную версию с окончательными пикселями. Это ещё
не доказывает показ этой версии на физическом iPad: квитанция показанного кадра,
сохранённое историческое изображение и успешный экспорт остаются разными фактами.

Ниже перечислены семь проверенных путей и их оставшиеся зависимости от архива;
также открыты единая пагинация документа,
окончательная проверка ограничений всей сцены, конвертер, перенос и физическая
приёмка. Установленные приложения и оригинальные данные не менялись.
Полный локальный маршрут прошёл; перечисленные открытые условия не закрываются успешной сборкой и тестами.

## Оставшаяся зависимость от размера архива

Карта ниже проверена по действующим вызовам 9 сентября. Расчёт размещения уже
переведён на ограниченный адресный путь; его остаточная граница указана отдельно.
Нативный пространственный контакт теперь тоже адресован. Две следующие
границы раскрывают глобальных владельцев, последние три — растущее содержимое
отдельного владельца или истории.
Ограниченное число возвращённых предметов само по себе не ограничивает чтение,
декодирование и память внутри транзакции.

| Владелец и действие | Оставшаяся работа | Проверка будущего закрытия |
|---|---|---|
| [CollaborationPlacement](../Sources/NotebookCore/CollaborationPlacement.swift): MCP рассчитывает место для композиции | Полный `collaborationSnapshot` удалён из production. `NotebookPlacementRead` читает геометрию и полную версию из одной SQL-транзакции; 4 096 препятствий, 16 МиБ исходника, 500 000 SQL-инструкций и 4 млн сравнений ограничивают работу. Произвольно большие элементы доски ещё могут исчерпать бюджет на далёком наборе; исходник одного листа пока читается целиком под ограничением. | `NotebookPlacementTests` сохраняет scope, стопки, бумагу Letter, окончательные чернила, CAS и отказ без частичного плана. В архиве 100 000 предметов область рассматривает одно препятствие и 333 SQL-инструкции; чужие обложки исключены адресным индексом. |
| [NotebookSpatialInkCommand](../Sources/NotebookCore/NotebookSpatialInkCommand.swift): нативный штрих или отмена на доске/обложке | Команда публикует один UUID; неизменяемые spans записываются один раз, отмена меняет причинный заголовок. `saveMergedSpatialInk` удалён. Собственная полная версия действия по-прежнему включает его точки; входящая репликация большого журнала относится к следующей строке. | `NotebookSpatialInkCommandTests`: среди 100 000 чужих действий чтение одного поддерева выполняет 98 SQL-инструкций, append с общей транзакцией — 1 144, undo — 939, echo — 58. Неизменность чужих хешей, rollback и повтор после commit проверены. |
| [NotebookStore](../Sources/NotebookCore/NotebookStore.swift): посадка создаёт следующий чистый лист | `saveWorkspaceSelection` с `createdPage` вызывает `publishWorkspace`: полный каталог, дерево и проверка зависимостей всех листов/документов. Нужна одна команда добавления адресованного листа с membership и выбором в `SessionPresence`. | Добавление листа в архиве из 100 000 предметов не читает чужое содержание; страница, её членство и выбор публикуются вместе, первая линия не обгоняет создание. |
| [NotebookReplicationStore](../Sources/NotebookCore/NotebookReplicationStore.swift): приходит изменение от связанного устройства | `applyRemoteChange` собирает все строки пакета в память и восстанавливает целиком каждый затронутый логический файл; изменение позиции раскрывает всё дерево и каталог. Нужны порционный обход пакета и причинное объединение затронутых UUID. | Маленький пакет в большом архиве не декодирует посторонних владельцев; старое эхо, независимые правки, повтор и сбой сохраняют прежние правила. Квитанция и входящий курсор появляются только после общей транзакции. |
| [SharedContext](../Sources/NotebookCore/SharedContext.swift), [AgentRequestStore](../Sources/NotebookCore/AgentRequestStore.swift): история, продолжение, вопрос и ответ | Обновление модели читает 64 полных контекста и квитанции со всеми изменениями. Append и завершение ответа перечитывают весь контекст. Квадратичный поиск `replyTo` устранён одним индексом UUID; отказ слияния больше не меняет принятую историю. Нужны заголовки, страницы записей и адресная проверка родителя одного нового сообщения. | `SharedContextValidationTests` проверяет 100 000 ответов, частичное продолжение, дубликаты, точный повтор и отказ без мутации. Остаётся: добавление одной записи без чтения всей переписки, ограниченная страница истории и подробности квитанции по ID. |
| [NotebookActionProjection](../Sources/NotebookCore/NotebookActionProjection.swift), [NotebookAgentCommand](../Sources/NotebookCore/NotebookAgentCommand.swift): apply, undo и продолжение хода | Каталог уже адресован, но projection включает ссылки всех записей контекста, целые страницы/документы и все действия выбранной ink-поверхности; также проходит всех предков доски. `agentExpectation` повторно читает тяжёлого владельца ради версии. Нужны заголовки и значения конкретных operation/receipt paths, без расширения разрешения агента. | Правка одного блока/элемента не читает посторонние программы и прежние указания; отмена сохраняет человеческую доработку, grant не расширяется, глубокая доска не требует полной проекции предков. |
| [NotebookSceneState](../Applications/Shared/NotebookSceneState.swift), [NotebookAddressedRead](../Sources/NotebookCore/NotebookAddressedRead.swift): выбор и соседний лист тетради | `readWorkspaceItem` восстанавливает все `pageIDs`; его вызывают scene read, working set, подготовка соседа и удаление. Выбору нужны существующие `readItemHeader`, `pageCount` и `pageID(at:)`; удаление может обходить только собственные страницы порциями. | В тетради со 100 000 листов выбор и подготовка соседа читают ограниченное число адресов; удаление имеет ограниченную память, удаляет все собственные страницы и не затрагивает чужие. |

Прежний полный снимок размещения остался только тестовой фикстурой, а не
запасным путём приложения. `placement_budget` означает явный отказ целого расчёта,
не пустую область. Точная версия снова проверяется при заказе карты чернил и
обычном применении результата. Отдельно выявлена граница `JSONValue`: число
Double не сохраняет произвольный Int64 тайла. Размещение теперь отклоняет
непредставимые координаты до записи render request; общий формат мировых
координат этим срезом ещё не исправлен.

Обычный выбор без нового листа, три нативные команды создания и удаление уже
используют адресные команды; тестовые вызовы старых bundle-методов не считаются
доказательством нагрузки работающего приложения.

## Открытая граница документа и живого окна

`DocumentWebCoordinator` уже исполняет интерактивные блоки только своего
физического листа, но каждый лист получает полный исходник, строит полный DOM
и повторяет первоначальную пагинацию. `DocumentRenderSession` теперь разделяет
неизменяемые исходник и состояние между листами: одно фоновое кодирование,
отдельная передача состояния без DOM и MathJax и одна принятая точная разметка.
Существующие WebKit остаются у координаторов; второго владельца записи или
дополнительного браузера сессия не создаёт. Дальнейший переход должен один раз
вычислять браузерную пагинацию и отдавать до четырёх настоящих листов WebKit
только их физические фрагменты. Выделение части большого Markdown
требует сравнения настоящих пикселей и accessibility; копирование целого блока
с обрезкой не устраняет размножение DOM. Интерактивный блок высотой до 2048
points, пересекающий несколько листов, остаётся отдельным обязательным случаем.

`IPadPageTurnController` теперь допускает строго четыре живых содержания.
После раскрытия направления он сохраняет готовые source, landing и направленного
соседа; внешние переходы во время движения заменяют только один ожидающий индекс.
После посадки исключённое содержание освобождается до создания нового. Позднее
подтверждение локальной страницы не теряет последний внешний переход; посадка
на чистый последний лист по-прежнему создаёт следующий. UIKit получает тот же
подготовленный экземпляр и может сохранять освобождённую оболочку.
`PageTurnSelectionTests` проверяет промежуточное окно, несколько дальних запросов,
отмену, возврат к источнику, устаревшую готовность и чистый лист.
`DocumentResourceLeaseTests` измеряет также кратковременный максимум настоящих
разрешений WebKit: пятый лист не создаётся. Целевой прогон 9 сентября в 13:57 МСК
прошёл **32/32** проверки без предупреждений; полный маршрут новых изменений
ещё предстоит. Общая пагинация остаётся открытой границей выше.

## Карточка вопроса у выделения

`NotebookQuestionOverlay` размещает прежнюю карточку у физических ссылок вопроса,
не меняя выбор, камеру или разрешение агента. Она предпочитает свободную соседнюю
область, избегает кнопок инструментов и остаётся над клавиатурой; при недостатке
высоты её содержание прокручивается. Смена ориентации сохраняет тот же ввод.
Если исходный предмет больше не виден, карточка остаётся доступной у края,
не возвращая человека к нему автоматически.
`NotebookInputGate` проверяет настоящие UIKit-границы карточки и кнопок инструментов
только в начале касания. Внутри них камера не начинает жест; снаружи остаются
камера и Pencil. Изменение рамки карточки не завершает уже начатый штрих.
Проверены семь геометрических и нативных регрессий, ввод вопроса без связи,
двухпальцевое касание карточки и поворот с открытой клавиатурой. Скриншоты обоих
положений сохранены в `.build/question-placement-20260909/` и просмотрены.
Это ещё не миниатюра закреплённого источника и не расширение доступа по согласию.

## Указание после живой правки чернил

`InkCanvasView` удерживает источник, установленный вместе с его готовой
геометрией. Принятые ручка и ластик добавляют в него свой настоящий UUID;
проверка не подменяет этот источник новой моделью или общими часами.
Один `SpatialInkSurfaceRegistry` обслуживает активную доску и живые обложки
во всём конечном дереве порталов. Освобождение прежней проекции не удаляет
регистрацию нового холста того же предмета. Неподготовленный или ещё активный
владелец не выдаёт источник как готовый.

Указание немедленно ставит границу в прежней очереди записи: предыдущий контакт
сохраняется до него, а следующий не обгоняет его и не объединяется через него.
`NotebookReferenceInkBasis` удерживает полные версии статической композиции
и вклад заменяемых живых чернил. Фоновое запечатывание подставляет точные действия,
включая отменённые, и сверяет источник с SQL; неподвижные слои остаются той же
показанной композиции. Это позволяет указать только что законченный штрих,
не ожидая повторной подготовки всей сцены. Передача в дочернюю доску учитывает
её собственную основу, даже когда тяжёлые чернила отсутствовали у родителя.

`SharedContext` повторно проверяет пространство и версии в транзакции записи.
Несовпадение возвращает явный отказ указания, не ошибку очереди нативного ввода.
Следующий штрих и камера продолжают работу; уже принятый вопрос сохраняет
первоначальные ссылки. Pencil внутри карточки не передаёт событие начала
владельцу чернил, а перенос карточки не отрезает контакт, начатый снаружи.

Семь `InstalledInkAttentionTests` проверяют ручку, ластик, отмену, задержанную SQL,
следующий контакт, передачу портала, вложенную обложку и смену камеры до отправки.
Три `NotebookReferenceInkTests` проверяют полные версии без чужого тяжёлого
содержания. `PortalInkTests` сравнивает настоящие пиксели действующего сборщика
с общим Metal; неиспользуемый `PortalBoardInkView` удалён.
9 сентября в 14:27 МСК прошли **90 iPad unit и три UI-сценария** без ошибок,
пропусков и предупреждений. Источник, установленный вместе с геометрией,
не является квитанцией представления GPU; полный PNG области и физическая
приёмка всё ещё открыты. Новый полный общий маршрут также ещё предстоит.

## Связь с 26 находками

«Интеграция» ниже обозначает реализованный механизм с указанными проверками,
а не успешный общий прогон последних исходников или приёмку физической пары.
«Срез» сохраняет границу ранее проверенного поведения; установленные приложения
этими изменениями не заменены.

| Находка | Механизм и исполняемая проверка | Граница |
|---|---|---|
| S1 | Причинный состав и явное удаление в SQL; выбор имеет одного владельца `SessionPresence`; `WorkspacePublicationTests`, `NotebookSQLiteTests` | Интеграция |
| S2 | Нативные изменения публикуют разницу адресной проекции и сохраняют невидимых владельцев; `NotebookSQLiteTests`, `NotebookPersistenceTests` | Интеграция; добавление чистого листа ещё читает полный каталог |
| S3 | Единственная транзакция SQLite WAL публикует содержание, версии, квитанцию и журнал; `NotebookSQLiteTests` проверяет rollback и неоднозначный ответ после commit | Интеграция; двойного файлового писателя нет |
| S4 | Создание предшествует первому штриху, ошибка удерживает очередь; `NotebookPersistenceTests` | Срез |
| S5 | Содержание, защита от повторов и входящий курсор публикуются вместе; `NotebookReplicationTests`, `AgentStateTests`, `MacModelLifecycleTests` | Интеграция; восстановление полных логических владельцев ещё осталось |
| S6 | Явное двустороннее сопряжение, UUID пространства и системный TLS; `NotebookTransportSessionTests`, `NearbySyncTests` | Интеграция; физическая пара ещё не принята |
| S7 | Дубликаты ID и неоднозначное физическое владение отклоняются; `WorkspacePublicationTests` | Срез |
| S8 | Ограниченные кадры и кредиты, дисковая сборка, страницы журнала и ACK после commit; `NearbySyncTests`, `NotebookTransportSessionTests`, `NotebookReplicationTests` | Интеграция; работа большого входящего пакета и срок хранения журнала ещё не ограничены |
| S9 | Контакт и камера принадлежат поколению соединения; поздний disconnect/ответ не восстанавливает старый ввод; `NotebookPersistenceTests`, `NotebookTransportSessionTests` | Интеграция; запись локального положения не входит в устойчивую репликацию |
| R1 | Пустой Metal-владелец готов без drawable/MSAA; `DrawingOwnershipTests` | Общий учёт drawable и текстур открыт |
| R2 | Регистрация и готовый mesh сохраняются при передаче; `SpatialInkProjectionTests`, `PortalPassageTests` | Срез |
| R3 | Учитываемые GPU-буферы и композиция SQL-порций в тайлы; `SpatialInkProjectionTests`, `SceneRenderResourcesTests`, `SceneCompositionSQLTests` | Интеграция; CPU-подготовка чернил и полная приёмка бюджета ещё открыты |
| R4 | Pencil отменяет pan, камеру и удерживаемую отмену; `NotebookInputTests` | Перенос/подъём обложки ещё требует общего допуска |
| R5 | Отмена pan сохраняет показанное положение; `NotebookInputTests` | Срез |
| R6 | Pan уступает контролам и редактированию; `NotebookInputTests` | Полное согласование всех владельцев жестов открыто |
| R7 | Отмена распространяется на подготовку, readback и публикацию; `DrawingOwnershipTests` | Уже отправленный GPU завершает владение ресурсами |
| R8 | Контакт фиксирует геометрию и маршрутизирует каждый сегмент один раз; `NotebookInputTests` | Срез |
| R9 | Совпавшие точки и коррекции меняют только хвост; `IncrementalInkMeshTests` | Срез |
| W1 | Состояние и соседний текст не пересоздают iframe; `DocumentRuntimeTests` | Срез |
| W2 | Устойчивый черновик, один активный редактор и source-CAS; `DocumentEditingSessionTests`, `DocumentRuntimeTests` | Конфликт сохраняет черновик |
| W3 | Состояние и перенос не перезагружают программу; `DocumentRuntimeTests`, `AgentWebLeaseTests` | Срез |
| W4 | Только программы своего листа; миниатюра заимствует реальные пиксели; `DocumentRuntimeTests` | Общая пагинация четырёх host ещё не реализована |
| W5 | Разметка без зависимости от скрытого rAF; `DocumentRuntimeTests`, проверка Mac-помощника | Неизвестное завершение программы не выдаётся за готовность |
| W6 | Разметка, пиксели и живое поколение iPad различаются; `DocumentRuntimeTests`, `SharedAttentionTests` | Физическая приёмка ещё нужна |
| Q1 | Запуск использует заголовок, окно сцены и ограниченное число тяжёлых владельцев; `NotebookSQLiteTests`, `WorkspaceSceneIndexTests` | Интеграция; растущее содержимое одного владельца и физический холодный запуск ещё открыты |
| Q2 | UI-тест касается видимого текста текущей страницы без повторов | Системные метрики и 30 минут на физическом iPad открыты |

## Дальнейшие границы перехода

Единственный владелец записи уже `NotebookStore` с SQLite WAL. Каталог, физическое
размещение, блоки, состояние, штрихи, история и квитанции хранятся адресными
записями; оставшиеся пути выше ещё восстанавливают из них избыточные полные значения.
Следующие изменения должны убрать эту работу, сохранив общую транзакцию
значений, причинных версий, отмены и журнала доставки.
`CollaborationStore` сохраняет правила действий и ожиданий; MCP обращается
к исполнителю Mac через IPC. Файловый писатель MCP, наблюдатель и прежний
сетевой протокол удалены; полного запасного пути публикации оставаться не должно.

Обзорные тайлы и запросы агента через существующий `SharedContext` проходят
интеграцию. Остаются одна сессия пагинации, проверка глобального бюджета ресурсов,
внешний конвертер и безопасный перенос пары. Запрос закрепляет источник и область
доступа; никакой текущий выбор не расширяет уже выданное разрешение.

## Карточка и ручки редактирования — 9 сентября

Повтор полного маршрута на `e2441d9` выявил, что карточка вопроса закрывала
ручку перемещения другого выбранного элемента. Результат **327/328 iPad**
сохранён как неуспешный; остальные проверки не превращают его в принятую пару.
`EditableElementContainer` теперь сообщает фактические рамки своих ручек
существующему владельцу размещения карточки. Вопрос остаётся закреплённым,
а перенос, размер и удаление доступны без ручного закрытия карточки.
Регрессия сохраняет исходный единственный drag, проверку человеческого
перемещения, рукописи и причинной отмены. Отдельно проверены поворот
с открытой клавиатурой и сохранение вопроса без Mac; полный общий маршрут
после интеграции последующих срезов всё ещё обязателен.

## Запрет преждевременной установки

Замена пары разрешается только после выполнения всех условий:

- Полный `verify.sh` без исключений и повторов, скрывающих неудачу; все 26
  воспроизведений и новые контракты имеют проверки.
- Внешний конвертер получил независимые окончательные снимки обоих устройств
  и подтверждение сохранения принятого ввода; отсутствие записи не трактуется
  как удаление. Приложения не содержат действующего legacy decoder.
- Закрытый checkpoint проверен и активируется атомарно; исходные внешние копии
  сохранены. После первой новой записи восстановление сохраняет новый формат.
- Проверены сбои транзакций, нехватка места, перестановки и повторы доставки,
  разрывы связи, отмена, остановка агента и выход за пределы разрешённой области.
- Измерены 1k/10k/100k уникальных владельцев и фактический архив. Работа кадра
  и рабочий набор не растут пропорционально архиву; обзор не подменён счётчиками.
- На физическом iPad выполнены холодный запуск, десять повторов маршрута и
  30 минут совместной работы. Проверены реальные системные представления кадров,
  CPU/GPU и память; `CADisplayLink` не используется как доказательство FPS.

До этой границы ни успешная сборка, ни отдельный зелёный набор тестов
не означают выполнения всего плана или отсутствия регрессий на устройстве.
