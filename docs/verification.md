# Проверенные контракты Notebook

## Состояние текущего среза

### Общий прогон Codex и нового рендера — 10 сентября, 18:59 МСК

**Полный маршрут не прошёл.** Зафиксированный исполняемый срез совпадает с
`b480dac`; независимая копия `.build/chat-full-verified` имеет Git tree
`31dfe92e1e5585756ba3371cd2a7168a7ff2189c` (от коммита отличаются только документы).
Все 487 проверяемых файлов остались неизменны; SHA маршрута
`ad2f4096b0713da6a2bf6820764ee069079f9ba5ce6ceb0e9c5890ad23769714`.
Журналы и результаты находятся в `.build/chat-full-integrated-20260910`;
`source-stability-on-failure.json` отдельно подтверждает отсутствие изменений
после отказа, но не является квитанцией успешного полного прохода.

Прошли **402 Core / 31 suites**, **26 Codex**, **23 external**, профильные
маршруты MCP, вычислений, распознавания и установщика, безоконный PNG Mac,
**95 Mac runtime** и сборка iPad Release. В iPad-проходе на Simulator
`24A434` — **449 из 459**, десять отказов, без пропусков и runtime warnings.
Девять относятся к интерфейсу: закрытие указания, переходы щипком/двойным
касанием, документ и навигация страниц. Один native-тест не получил готовую
композицию плотной доски до измерения камеры. Это не доказательство плохой
частоты кадров: до самого измерения тест не дошёл.

Отдельная диагностика связывает потерю кнопки закрытия с контейнером доступности
карточки, а неполученный щипок — с реальным перекрытием нижних контролов панелью
чата: первый синтезированный палец `(762.80, 1121.66)` попал внутрь панели
`(682, 1070, 120, 72)` и был правильно отклонён владельцем ввода. Пиксели,
дерево доступности и событие сохранены в `.build/chat-ui-diagnostic-attachments`.
Это причины для исправления интерфейса, не для ослабления защиты ввода,
изменения жеста в тесте или увеличения таймаута. Отдельный профиль камеры
на том же `b480dac` прошёл 2/2; причина её общего отказа ещё не установлена.
Установленная пара не заменялась; физическая приёмка этим прогоном не выполнена.

Исправление интерфейса проверено отдельно на `b480dac` с тремя изменёнными
файлами (`NotebookRootView`, `NotebookAgentQuestion`, UI-тест): **19/19**,
240.575 s, без пропусков. Все девять прежних UI-отказов прошли; также проверены
живой ввод с поворотом, рамки свёрнутой/раскрытой панели в двух ориентациях и
восемь контрактов размещения указания. Панель занимает свободную правую область
между фактическими рамками верхних инструментов и нижнего подключения; камера
и защита контакта не изменялись. Кнопки карточки принадлежат её явному контейнеру
доступности. `.build/chat-controls-profile.xcresult` сохраняет результат.
Этот профиль не заменяет новый полный маршрут. Захват новых системных пикселей
поворота проверен отдельно от AX-геометрии: `app.screenshot()` оставил старую
ориентацию и обрезал правую часть, поэтому используется `XCUIScreen.main`.
На окончательном UI-источнике этот тест прошёл **1/1**, 29.705 s;
`.build/chat-controls-screen-profile.xcresult`. Просмотрены полные системные
снимки раскрытой панели landscape и свёрнутой portrait: нижние кнопки видимы,
панель не пересекает инструменты, лист сохраняет прежнюю геометрию.

### Объединение с новым Notebook Lab — 10 сентября 2026

Последнее требование Амира — оставить **новое** приложение и удалить старое
только после переноса. Новое приложение сейчас имеет bundle ID
`com.amirtlinov.notebook.preview`; старое — `com.amirtlinov.notebook`.
Проверка устройства в 12:44 МСК видит оба приложения и оба процесса:
Lab PID 11828, старое PID 11829. Ни один из них не заменён и не удалён.

Внешний конвертер теперь умеет добавить старые записи в полную независимую
копию нового архива, не теряя его собственные запросы, остановки и исходники.
Пробное объединение прежних копий дало **19 предметов, 54 листа, 7 документов
и 655 пространственных действий**. Сохранена идентичность Lab
`17248CEA-63FD-42D3-AC4F-D6C5F13DED14`, его выбор листа и контекста.
**567 существующих записей** вне объединяемых каталога, дерева и чернил сохранили
адреса и хеши. Включая запрос `4FC1519F-4A66-49F2-A13F-A2DB38E6DE89`, его
закреплённый исходник и остановку: импорт не создал исполнение этого запроса.

На 19 листах выбран PNG старого iPad после проверки совпадения сохранённых
PencilKit-байтов, размера, версии и отсутствия последующих действий обеих основ.
Оба исходных PNG и исходный PencilKit остаются в независимых копиях; их хеши
перечислены в отчёте. Это не утверждение о равенстве PNG. Положения предметов,
тексты, программы, точки и история не переписывались. Корневое членство
импортированных предметов и общий порядок получили собственную версию импорта.

Проверенный кандидат — `combined-lab-verified` внутри папки резервных копий ниже.
Он создан executable проверенного среза и полностью прочитан обратно; SQLite
`integrity_check = ok`, нарушений внешних ключей нет. SHA содержания —
`fdcfa40cd7da0df27b00dbd3a619ccf76b36c4cf5c4d2133570bb591c3a0cb13`.
Отчёт — `combined-lab-verified/report.json`, независимый итог —
`.build/archive-consolidation-20260910/final-readback.json`.

Дополнительная проверка настоящим `PKDrawing` подтвердила исходные данные
и число штрихов на всех 19 листах. Внешние тесты не создают рисунок внутри
безымянного SwiftPM-процесса: у конструктора PencilKit обнаружена зависимость
от bundle ID для собственных настроек реплики. Синтетическая линия создаётся
отдельным воспроизводимым генератором с собственной идентичностью и временным
домашним каталогом; тест и конвертер только декодируют её. Сохранены диагностика
этой ошибки и исправленной ранее некорректной точки board-фикстуры. Это исправления
причин сбоев, не маскирующие повторные запуски. Промежуточный полный прогон
остановлен до усиления проверки происхождения и не засчитан.

На неизменном наборе **415 файлов реализации и фикстур** прошли **356 Core +
21 внешняя проверка** (полный `swift test`, 758,883 секунды), затем **97 Mac
runtime-тестов** без предупреждений и пропусков, TypeScript check, **36 MCP unit
и 10 smoke** с отдельными синтетическими архивами и IPC-процессами. SHA набора —
`c2a20c77ffb33ce006dbb39864038ac215c3e6ce4a84a982325403c2664a972f`,
`changed: []`. Полные логи, исходные манифесты и квитанции лежат в
`.build/archive-consolidation-20260910/`; Mac — в
`.build/native-lifecycle-20260910/archive-consolidation-mac/`.
Во время Core-прогона другая задача добавила три независимых Python-файла
`Tests/NotebookCodexBridgeHarness`; они не входят в эти 415 файлов и не меняют
Swift-targets. Mac-манифест содержит уже 418 файлов и также имеет `changed: []`.
Чужой срез не выдаётся за проверенный этой задачей. Полный `verify.sh` и физическая
приёмка для объединения не запускались. Живой MCP в 13:14 МСК всё ещё вернул
`ipc_unavailable`; проверенные изолированные MCP-тесты не означают восстановление
установленного помощника.

**На iPad перенос ещё не выполнен.** Копии сняты до разрешённого возобновления
рисования. Старые процессы не предоставляют доказательство сохранения всех
принятых UUID и точек, а атомарная активация и восстановление пары ещё не
реализованы. Доказанный offline readback не заменяет эти условия, физический
показ или полный успешный `verify.sh`. Рабочий MCP этим срезом не восстановлен.

### Активация проверенной пары — 10 сентября 2026, 16:46 МСК

`NotebookArchiveActivation` и внешний `--prepare-pair` проверены на отдельном
неизменном наборе исходников `.build/activation-verified`, собранном от
`035ac6d` плюс изменения активации и точных координат. Команда
`swift test --filter 'NotebookArchiveActivationTests|ArchivePairPreparationTests|WorldPointCodingTests'`
прошла: 16 Core-проверок и 2 проверки внешнего конвертера; журнал
`.build/activation-staged-tests.log`. В этом же наборе `npm run check` и
`node --import tsx --test test/spatial.test.ts` прошли, 3 из 3 тестов.

Проверены независимая подготовка, атомарный обмен, прерывания, отказ при
изменённом источнике, повреждении, чужой идентичности и нехватке места,
повтор, допуск по квитанциям обоих устройств и сохранение новой записи
после активации. Реплика Mac сохраняет остановленный вопрос и закреплённый
источник, но не получает чужие локальные задания, actor и сетевые курсоры.
Отпечаток общего содержания включает адрес, хеш и положение каждой записи.

Три `NotebookArchiveLaunchTests` прошли в совместном native iPad-профиле:
ошибка или отсутствие второго допуска не создаёт модель; изолированный
запуск не регистрирует производственные инструменты Codex. Это профильная
проверка текущей совместной разработки, не полный `verify.sh` на неизменном
релизном дереве. Системный отказ InputUI ниже не отменён. Настоящие архивы
этим срезом не активированы, приложения не установлены и не удалены.

### Ограниченная работа транзакции — 10 сентября 2026, 17:18 МСК

Временные таблицы SQLite заменили накопители изменений и зависимостей в Swift.
`NotebookPendingChangesTests` проверяет 100 000 изменений, семь упорядоченных
частей журнала, 100 000 зависимостей, сохранение первого источника, повтор и
откат. Новый SQL-сеанс не получает временную работу прежнего. Размер файлового
кеша — 2 МиБ, страница обхода зависимостей — 64 адреса.

На неизменном дереве `0b0ddb952daa4fc9999053dc50246d61fb292a9b`
в `.build/pending-verified-v3` прошли все **384 Core** (773,925 секунды) и
**25 Codex** проверок. Общий `swift test` тогда завершился отказом: внешний
тест симлинка ожидал прежний тип ошибки конвертера, хотя файловую проверку
теперь исполняет Core. Первая правка задела соседнее ожидание и дала два
отказа; оба журнала сохранены. После точной правки проверяется сам отказ
`archive contains a link or special file`, а неизвестный владелец по-прежнему
отклоняется конвертером до создания назначения.

На изолированном интеграционном дереве от `ebbbe43` с этим срезом прошли
**38 Core** проверок (135,303 секунды) и все **23 внешние** проверки
(0,823 секунды). Лог — `.build/pending-final-integration-v2.log`;
исходный полный прогон — `.build/pending-full-core-v3.log`. Это проверка
транзакционного механизма, не полный `verify.sh`, не адресное входящее
объединение всего каталога и не физическая приёмка.

### Системная клавиатура на Simulator 24A434 — 10 сентября 2026, 17:35 МСК

Официальный runtime 24A434 установлен рядом с 24A5408d. Xcode остаётся
27.0 beta 5; глобальное сопоставление runtime не менялось. Новое тестовое
устройство создано по абсолютному пути 24A434; `getenv` и сам `xcresult`
подтверждают этот build для UUID `CBDE7503-A0F7-4B92-85B5-626B9375E0FA`.
Независимый `StockInputControl` со стандартным TextField прошёл 1/1.
Доказательства — `.build/stock-inputui-rc-20260910/`.

Усиленный сценарий Notebook держит клавиатуру открытой при повороте, проверяет
геометрию окна, клавиатуры и панели, сохранность черновика после сворачивания
и дописывание текста в landscape. Первая проверка ошибочно считала, что касание
поля ставит курсор в конец; оно выбрало слово. Этот отрицательный результат
сохранён в `.build/chat-keyboard-rc-20260910/`. После окончания теста остановлен
зависший сбор `simctl diagnose`, поэтому итог запуска — interrupted, не pass.
Исправленный тест явно перемещает курсор системной Cmd+Right перед дописыванием.

В `.build/chat-keyboard-rc-positioned-20260910/` исправленный сценарий прошёл
1/1 без пропусков, exit 0, 19,513 секунды. Снимок визуально проверен: полный
черновик виден над открытой клавиатурой. Автокоррекция не отключалась,
геометрические ожидания не ослаблялись; диагностический снимок создаётся
перед утверждением о повороте. Это целевой профиль на новом runtime, а не
полный успешный `verify.sh` или приёмка физического iPad.

### Внешняя подготовка архивов — 10 сентября 2026

Добавлен отдельный executable `notebook-archive-transfer`; его старые декодеры
не связаны с приложениями и MCP. `NotebookCheckpoint` и обычная транзакция
`NotebookStore` принимают только полный новый архив в пустое назначение.
Установленные процессы, базы и приложения не заменялись; рабочий MCP всё ещё
не восстановлен. Подробный контракт и запреты — в [archive-transfer.md](archive-transfer.md).

С разрешения Амира после паузы сняты отдельные копии основного iPad, Lab и Mac.
Внешняя папка — `/Users/amir/Documents/Notebook Backups/2026-09-10-1122-pre-migration`.
`backup-sha256.json` фиксирует 3 252 скопированных файла, включая первоначальные
и снятые после паузы копии; это доказательство сохранённых файлов, не полного
состава принятого ввода в памяти старых процессов. После копирования Амиру
сообщено, что можно снова рисовать: эти снимки **не являются окончательными**.

Из копий основного iPad и Mac подготовлены два независимых новых архива:
по **15 предметов, 50 листов и 6 документов**. Квитанции и отчёты лежат в
`prepared-ipad-main-verified/report.json` и `prepared-mac-verified/report.json` внутри папки копий.
Это два кандидата одного назначения, **не согласованный checkpoint пары**.
Идентичность назначения — `1FE430B7-0CE2-412F-886A-DA040D1A56F7`.

- iPad: 654 пространственных действия; checkpoint SHA-256
  `2ec8cbd23362e7d2439ecd5804cc2f972bf2f966765a2b48cd3eceb2aa0b2770`.
- Mac: 576 пространственных действий; checkpoint SHA-256
  `092eb334577c4bce58b2caf9ff36f37a3d5320da3aa0d3b001c90efff41a5303`.
- Каждый опубликован одной транзакцией, полностью прочитан обратно и совпал
  с подготовленными владельцами. У обеих SQLite `integrity_check = ok`, нет
  нарушений внешних ключей, в журнале ровно одна публикация. Исходные хеши
  повторно совпали перед выдачей результата.
- В каждой копии сохранены 26 контекстов, 21 действие и 21 квитанция доставки.
  Девять прежних пустых служебных записей агента связаны с точными квитанциями
  и получили уже существующие описания действий. UUID, даты, авторы и счётчики
  остались прежними; соответствия перечислены в отчётах.
- 19 непустых листов переведены из бинарного NotebookInk/1 в NotebookInk/2
  без изменения байтов PNG основы. На Mac девять файлов листов вне каталога
  оставлены в исходной копии и перечислены отдельно, а не возвращены в тетради.

Сравнение двух копий подтвердило одинаковые каталог, доски, документы, состояния
и историю. Все 576 UUID чернил Mac есть на iPad с теми же неизменяемыми точками;
там дополнительно 78 действий и одна более поздняя причинная отмена.
Сохранённые до прежней миграции PencilKit-байты совпадают на всех 19 непустых
листах. У 17 листов совпадают также декодированные пиксели; у двух отличаются
только значения alpha на единицу из 255. Это не объявлено равенством PNG и не
разрешено молчаливым выбором по одинаковым часам.

Копия Lab проверена отдельно: SQLite schema 2, `quick_check = ok`, собственная
идентичность рабочего пространства. При подготовке этих двух кандидатов она
ещё не объединялась с основным Notebook; последующее объединение описано выше.
Скрипт проверки копий, JSON-аудит, логи конвертации и тестов находятся в
`.build/archive-transfer-20260910/`. Настоящие копии не стали runtime-фикстурами:
тесты используют синтетические данные, а чтение копий выполнял только внешний
конвертер и отдельная проверка происхождения.

На неизменных **408 файлах** прошли **352 Core + 9 внешних проверок**, TypeScript
check, **36 MCP unit + 10 smoke**, затем **97 Mac runtime-тестов** без пропусков
и предупреждений. Все проверки MCP использовали собственные IPC и синтетические
архивы. Манифест исходников — `75da37e4fea538f551d750e40211da397589d1ece24dfdb7307c61db59c3d318`,
`changed: []`; сводка Mac и `.xcresult` находятся в
`.build/native-lifecycle-20260910/archive-transfer-mac/`. Внешняя конвертация обеих
копий повторена executable из этого проверенного набора исходников в новые
каталоги с суффиксом `-verified`; первоначальные кандидаты не перезаписывались.
Полный `verify.sh` и физические сценарии для этого среза не запускались.
Повторный живой `notebook_observe` в 12:13 МСК вернул `ipc_unavailable`.
Уже запущенный MCP-процесс ещё показал прежнее сообщение; исправленный текст
проверен в новых изолированных процессах. Установленный Mac сохранил тот же
PID и SHA-256, сокет отсутствует. Перезапуск MCP не добавит IPC старому приложению.

Уточнённое требование Амира — оставить новое приложение, сейчас Notebook Lab,
после полного переноса удалить старое. Обе установки пока сохранены: нельзя
терять ни старое содержание, ни новые записи и запрос агента.

Остаются доказанный drain старого принятого ввода, свежий окончательный снимок,
согласование одного checkpoint пары, безопасная активация и физическая приёмка.
Успешное внешнее преобразование не снимает эти условия и не заменяет текущий
полный `verify.sh`.

### Рабочее подключение MCP — 10 сентября 2026, 11:13 МСК

Живой `notebook_observe` вернул `ipc_unavailable`. Сервер `notebook` включён
в Codex и запускает `MCP/run.sh` текущего checkout; инструменты присутствуют.
Процесс `/Users/amir/Applications/Notebook.app/Contents/MacOS/Notebook` запущен,
но `/tmp/notebook-501/bridge.sock` и его каталог отсутствуют. Это не сбой
разрешений агента и не доказательство, что процесс приложения остановлен.

Установленный Mac bundle имеет версию 0.3.14 (17), исполняемый файл от
6 сентября; SHA-256 `04c2dddc492078465cc3ab97713304ae4a13ec56c393157aff1e7c6ee714f92b`,
Mach-O UUID `11C3CDB3-61DC-3CC3-A764-0E2B67AADB12`. В нём нет IPC-сервера;
его действующий процесс наблюдает прежние файловые каталоги. MCP переключён
на локальный сокет в `fc3cd99`, а установленный помощник не заменялся.
Одинаковый номер версии в package и bundle не подтверждает совместимость.

В исходниках сообщение отсутствующего IPC больше не утверждает «helper не запущен»:
оно требует проверить запуск **совместимой** сборки. Это исправление
диагностики, а не восстановление рабочего доступа. После него TypeScript check,
36 unit и 10 smoke-сценариев прошли с отдельными тестовыми хранилищами и IPC;
лог — `.build/mcp-connection-diagnosis-20260910/diagnostics-check.log`.
Прежний файловый писатель не возвращался, новая модель над настоящим архивом
не запускалась. Рабочее подключение требует завершённого внешнего переноса
и согласованной замены пары по карте перехода; простой перезапуск Codex или
старого Notebook этой несовместимости не исправляет.

### Снятие указания — 10 сентября 2026

`NotebookAgentQuestion` владеет одновременно карточкой и человеческой рамкой.
Закрытие сразу снимает обе поверхности и ставит снятие общего выбора в ту же
очередь, что принятое указание. Сохранённая история больше не рисуется как
постоянное выделение. Позднее завершение указания, обновление из SQLite и
перезапуск не открывают закрытую карточку. Сам вопрос, его разрешение и история
остаются; явное продолжение из истории возвращает исходные ссылки.

До исправления два новых native-теста воспроизвели сохранённый выбор и его
возврат при перезапуске. Отдельный UI-тест нажал настоящий крестик: карточка
исчезла, но 23,44% пикселей полосы вдоль нижней границы рамки отличались от
листа до указания. Это отказ по оставшемуся выделению, не по наличию карточки.

После исправления прошли **17 native и 3 UI-проверки**, без пропусков и runtime-
предупреждений. UI дважды показал и снял рамку, между ними продолжив тот же
фрагмент из истории; исчезновение проверено по пикселям, рукопись не изменилась.
Просмотрены снимки до исправления и после закрытия. Свидетельства находятся в
`.build/native-lifecycle-20260910/attention-dismiss-{red,ui-red,green}/`:
`native.xcresult`, `summary.json`, `receipt.json`, парные манифесты исходников;
экспортированные кадры — в `attention-dismiss-green/screenshots/`.
Каждый маршрут шёл на неизменных 402 файлах, `changed: []`.
Затем на тех же исходниках прошли **97/97 Mac-проверок**, без пропусков и
runtime-предупреждений; сводка, результат и парные манифесты сохранены в
`attention-dismiss-mac/`. Общая модель и представления собираются на обеих
платформах; суммарно проверено 117 тестов.

Полный `verify.sh` для исправления выделения не запускался; результат ниже
относится к предшествующему срезу, а не к изменённым исходникам. Этот локальный
срез не разрешает замену установленной пары. Физические iPad, Notebook Lab и
Mac не обновлялись.

### Последний полный маршрут, до исправления указания

Полный маршрут предшествующего среза завершился **10 сентября 2026 в 10:42 МСК**
с exit 65: один системный UI-тест не прошёл. Это не зелёный маршрут и не
разрешение установки. Основной Notebook, установленный Notebook Lab и Mac
не заменялись. Условия полной реализации остаются в [карте перехода](reliability-transition.md).

Проверялась отдельная неизменная копия `.build/addressed-pages-verified-20260910/`.
Весь процесс занял 1939,990 секунды; `source-postcheck.json` подтверждает
418 неизменных файлов копии. Манифест исполнения `evidence/source-before.json`:
402 файла, SHA-256
`f5a71681537fd0abfd39181c7c729af18cd73f4838903e3db035d8c8cee3f328`.
После диагностики `evidence/worktree-receipt.json` сверяет тот же runtime с
рабочим деревом. Финальный этап самого `verify.sh` после отказа не исполнялся.

| Маршрут | Фактический результат |
|---|---|
| Core | 346/346 в 22 наборах, 706,469 секунды; включены проверки на 100 000 владельцев. |
| Фикстура, иконки, первая установка | Проверки исходников, seed, форматов и иконок прошли; 38 проверок защиты установщика прошли. Установки не было. |
| MCP | TypeScript check, 36/36 unit и 10/10 smoke через изолированный IPC. |
| Исполнитель | 8 проверок закреплённого App Server с mock-provider и 6 сценариев координатора с SQL прошли. |
| Mac | 97/97, без пропусков и runtime-предупреждений. Безоконный запуск создал PNG: 589 082 байта, SHA-256 `10f662d8a1c9c22cd065af3f6231491efbe21d802c7a69c2da7cc648dd634d17`. |
| iPad | Release Simulator собран. Native: 406/406. UI: 38/39; всего 444 успешных и один отказ, без пропусков и runtime-предупреждений XCTest. |

Среда: Xcode 27 beta 5, macOS 27.0 `26A5421a`, iOS 27.0 Simulator
`24A5408d`, iPad Air 11-inch M4. Сводки и дерево тестов лежат в
`evidence/mac-summary.json`, `ipad-summary.json` и соответствующих `*-tests.json`.
Просмотрены настоящий PNG Mac и снимок разных миниатюр страниц из успешного
`testPageControlsAndSearchReturnToTheReadPage`.

### Отказ системной клавиатуры

`testQuestionDraftSurvivesRotationAndRemainsReachableAboveKeyboard` не дождался
клавиатуры после поворота. Запись показывает сохранённый черновик и исчезнувшую
клавиатуру. В тот же момент аварийно завершился **системный `InputUI`**, SIGABRT:
`TUIKeyboardCandidateMultiplexer` → `TUIInputSession.pushAutocorrections` →
`UIView.setHidden` → `UIFocusSystem` → assertion UIApplication на фоновой очереди.
Отчёт `InputUI-2026-09-10-104029.ips` сохранён в `evidence/inputui-crash/`.

Независимый контроль `.build/stock-inputui-20260910/` содержит только стандартный
SwiftUI TextField: ни моделей, ни рендера, ни файлов Notebook в его сборке нет.
Он воспроизводит тот же отказ и SIGABRT `InputUI` с той же цепочкой;
отчёт `InputUI-2026-09-10-105421.ips` сохранён рядом. Его `control.xcresult`,
`exit.txt` и `source-manifest.json` фиксируют контроль, а не успешную приёмку.
Предположения о дополнительном окне и его уровне не подтвердились; изменения
полностью удалены, исходный runtime восстановлен до проверенного манифеста.
Автокоррекция не отключалась, проверки и ожидания не ослаблялись. Повтор после
crash не выполнялся ради зелёного результата. Для полного прохода нужна
исправленная среда системного ввода и новый общий прогон, не обход в Notebook.

## Адресное чтение тетради

`NotebookSceneState` получает заголовок, UUID выбранного листа, его позицию,
корень порядка и до четырёх тел в одном WAL-снимке. `NotebookAppModel` хранит
только эти подготовленные UUID; частичный `pageIDs` не задаёт номера или
количество страниц. Обновление камеры и чтение внешних изменений сохраняют
подготовленную дальнюю цель, заново разрешая её UUID в текущем порядке.

Кнопка навигации закрепляет корень до ожидания принятого Pencil. UIKit
удерживает поколение порядка и физические идентичности подготовленных hosts;
старые запросы соседа, готовность и завершение анимации не выбирают новую
страницу на прежнем месте. Адресная посадка возвращает опубликованный корень:
конкурентное добавление не меняет UUID принятого листа, а модель уточняет его
настоящую позицию после commit. Сохранение чернил на вытесненной странице не
возвращает её тело в ограниченный рабочий набор.

MCP использует заголовки и те же адресные окна. `notebook_read_notebook`
возвращает до четырёх строк метаданных без тел листов; продолжение с `page_order`
отказывает при изменённом порядке. UUID, номер, корень и точная десятичная строка
курсора приходят из одного чтения. Суммарный предел всех окон одного запроса —
четыре тела. Прежние команды полного каталога и Core-методы полного предмета
удалены, а не оставлены как запасной путь; фикстуры используют заголовки и окна.

Удаление получает не частичный состав UI, а собственные SQL-членства порциями
по 64. Каждый адресный подграф также удаляется порциями, без декодирования
чужих или повреждённых ненужных тел. Это не доказывает постоянную память всей
транзакции: накопитель изменений и исходящий журнал остаются отдельным
незавершённым условием.

## Решающие проверки

- `NotebookPageWindowTests`: точные wire-курсоры, запрет прежних команд,
  совокупный предел четырёх тел, метаданные без содержания, отказ по старому
  корню, rollback удаления 129 страниц и сохранность другой тетради.
- `NotebookPageAppendTests` и `NotebookPageWindowTests`: 100 000 листов,
  индексный SQL и неизменяемый порядок; проекция читает только точные причинные
  поля представленных UUID. Порча ненужного тела не ломает адресное чтение.
- `NotebookPageAddressTests`: настоящий iPad runtime на Simulator запускает
  модель с 100 000 листов и испорченным ненужным членством, выбирает 77 778-й,
  переходит по UUID на последний и добавляет 100 001-й. Четыре тела и до пяти
  UUID проекции выбранной тетради сохраняются; старый корень не разрешает выбор.
- Отдельные native-проверки сохраняют дальний UUID после обновления покрытия
  и полного обновления сцены; конкурентное добавление находит принятую страницу
  на её фактическом месте. `PageTurnSelectionTests` отвергает callbacks старых
  физических оболочек, включая поздний запрос соседа.
- `AcceptedPageInputTests` удерживает принятый штрих и причинную отмену после
  настоящего вытеснения исходной страницы; сохранение не раздувает кеш тел.
- MCP unit и smoke проходят через собственный изолированный IPC-процесс,
  проверяют метаданные, продолжение и отказ по чужому порядку. Архивный
  исполнитель из этого исторического профиля удалён; текущий маршрут ниже.

Целевые native-запуски лежат в `.build/native-lifecycle-20260910/`.
`addressed-page-regressions/` обнаружил потерю дальнего UUID при обновлении
сцены; после исправления `addressed-page-regressions2/` прошёл 58/58.
`addressed-page-callbacks/` дополнительно проверил поздний запрос UIKit:
20/20. Это локальные доказательства причин, не замена полного маршрута. На окончательном
runtime `addressed-page-mac-final/` прошёл 97/97, `addressed-page-native-final/` —
58/58; общий прогон отдельно подтверждает все три проверки `NotebookPageAddressTests`.

## Граница физической приёмки и установленных данных

`verify.sh` последовательно проверяет Core, нагрузочную фикстуру, защиту
первой установки Lab, иконки, MCP и smoke, протокол адаптера Codex,
Mac runtime, безоконный PNG, Release Simulator и весь iPad
runtime/UI. Манифесты до и после связывают результат с одним набором исходников.
Ни один helper не запускается над прежним человеческим архивом.

Simulator не подтверждает системное представление кадра на физическом iPad,
задержку Pencil, полный RSS или готовность настоящего архива на 100 000 предметов.
CADisplayLink — частота callback, не FPS. Нужны CPU/GPU, реальные представления
кадров, десять повторов маршрута и 30 минут совместной работы. Контрактные
тесты адаптера не удостоверяют настоящую работу установленной пары.

В установленном Lab уже есть человеческий ввод. Старая пустая SQL-очередь и
равенство часов не доказывают сохранение всех UUID; новая модель не исправляет
память прежнего процесса задним числом. До замены нужны независимые внешние
снимки всех трёх архивов после согласованной паузы, проверка обнаруженных данных,
визуальная сверка Амира, проверенный checkpoint и атомарная активация. Это
согласованный критерий исторического ввода, не программный учёт каждого старого
UUID. `install-preview.sh` отказывает при существующем Lab;
прежний установщик пары удалён. Настоящие приложения, процессы и данные этой
работой не заменялись.

## Адресная история и системный ввод — 10 сентября, 18:01 МСК

Неизменный кандидат `8ea9dad25920d840bc65b530b0c54904f6989d6f` построен поверх
`16443b3`; его копия — `.build/context-cursor-verified`. После исправления владельца
курсора прошли 62 Core-проверки в четырёх группах и 12 внешних проверок объединения
(`.build/context-cursor-integration.log`), TypeScript check, 40 MCP-проверок и
10 smoke-сценариев через изолированный IPC (`.build/context-cursor-mcp-*.log`).
Предшествующий immutable Core-срез прошёл 400 + 25 Codex + 23 внешних проверки
(`.build/context-full-swift.log`); он не подменяет проверку окончательного кандидата.

`SharedContextReadTests` содержит 100 000 записей с намеренно повреждённым
ненужным телом. Добавление публикует ровно одну запись журнала; ограниченные
чтения проходят под SQL-пределом. Проверены точный родитель, отсутствие частичной
публикации при отказе, продолжение шестимегабайтной истории, старый выбранный
контекст вне первого окна и независимость курсора от выбора и чужих записей.

Native-профиль прошёл 6 `SharedAttentionTests` и один системный сценарий истории:
старые фрагменты, следующая страница ответов и возврат к началу. Квитанция —
`.build/context-cursor-native-result.xcresult`; изображение `addressed-context-history`
просмотрено. Два прежних отказа сохранены: идентификатор контейнера перекрывал
кнопки продолжения, затем посторонняя запись меняла общий курсор. Исправлены
владельцы контролов и ревизии; таймауты и проверки не ослаблены.

Клавиатура проверена в Simulator `CBDE7503-A0F7-4B92-85B5-626B9375E0FA`,
реальный runtime build `24A434`, Xcode `27A5237l`. Независимое стандартное поле
также проходит на этом runtime. Вариант с Command-Right ранее прошёл отдельно,
но отказал в общем профиле: слово осталось выбранным. Текущий сценарий проверяет
геометрию уже открытой клавиатуры и сохранность черновика после поворота, затем
обычным касанием ставит каретку за коротким текстом и проверяет точное дописывание.
Он прошёл десять повторов с перезапуском test runner и остановкой при первом
отказе, без пропусков: `.build/context-cursor-keyboard-10-result.xcresult`, exit 0.
Автокоррекция включена. Это десять повторов Simulator, не физическая приёмка iPad.
Полный `verify.sh` этого кандидата выполняется отдельно; установленная пара
и прежние архивы не заменены.

## Переиспользование графической памяти — 10 сентября, 18:25 МСК

Неизменное дерево `31dfe92e1e5585756ba3371cd2a7168a7ff2189c` построено поверх
`d5896ea`, копия — `.build/tile-pool-final-verified`. Расширенный iPad runtime
прошёл **100/100** за 51,154 с (`.build/tile-pool-final-native-result.xcresult`),
весь Mac runtime — **95/95** за 75,401 с
(`.build/tile-pool-final-mac-ready-result.xcresult`); оба процесса завершились с 0.
Первый Mac build остановился до тестов из-за отсутствующего `npm ci` в новой
копии. Зависимости установлены по lockfile; исходники не менялись. Это не повтор
XCTest после отказа. Общий `verify.sh` того же кандидата выполняется отдельно.

Один InkCanvas теперь держит пулы по 512 pixels. В каждом — показанный drawable
и один следующий; только один нативный кадр отправляется на GPU одновременно.
Источник, геометрия и общий encoder прежние. Подготовка перекладки рисует в
следующие drawable без изменения показанных слоёв; рост добавляет только новые
пулы. Отмена отзывает частный слот после GPU, а окончание CA освобождает
квитанцию от владения слотами и снятыми пулами. Нехватка памяти сохраняет
старый целый кадр и не допускает контакт вне его backing.

Проверены десять перекладок при заполненном общем бюджете, где новая полная
копия заведомо не поместилась бы; идентичности пулов и количество байтов
остаются прежними. Каждый центральный пиксель штриха через границы тайлов
проверяется после каждой установки. Рост, отмена, удержанная старая квитанция,
новый Pencil во время подготовки и освобождение после stop имеют отдельные
проверки. Backing 4400×256 сохраняет 2× плотность вместо прежнего скрытого
снижения по пределу 4096. Первый штрих на 12,9/13-inch проходит рядом с полностью
занятыми 128 МиБ пассивной половины. Снимки `near-full-retina-tile-rotation` и
`large-retina-first-line` из финальной квитанции просмотрены.

Первый вариант с тремя drawable на тайл превышал бюджет больших iPad; он не
принят. В том же отрицательном запуске новая фикстура ошибочно создала запрещённую
nonfinite SpatialPoint и аварийно завершилась; автоматический перезапуск XCTest
не засчитан. Теперь недопустимая плотность проверяется через её настоящую границу
входа. `.build/tile-pool-native-result.xcresult` и `.build/tile-pool-native-status.txt`
сохраняют оба отказа. Исправленный промежуточный профиль прошёл 28/28 до усиления
пиксельных проверок. Ни один результат Simulator не подтверждает реальные
CPU/GPU, RSS, задержку Pencil или 30 минут работы физического iPad.
