# Автоматическое подключение установленной пары

Установщик, которому пользователь разрешил настроить свой Mac и уже доверенный Apple CoreDevice iPad, собирает две настоящие квитанции активации. `notebook-archive-transfer --prepare-installation-pairing IPAD_RECEIPT MAC_RECEIPT --output NEW_PRIVATE_FILE` создаёт одноразовое разрешение именно этой пары: обе квитанции, случайный UUID и общий случайный ключ 256 бит. Команда не меняет приложения или доверие самостоятельно. Выход создаётся атомарно с правами 0600; существующее разрешение нельзя перезаписать. Его передают только в закрытые контейнеры названных приложений под именем `Notebook.activation/installation-pairing.json`, никогда через Bonjour, MCP, журнал содержимого или git.

`NotebookApplicationLaunch` принимает разрешение после допуска обеих активаций, но до создания модели, сети и агента. `NotebookArchiveFiles` ограничивает размер чтения и запрещает ссылки; `NotebookInstallationPairingGrant` сверяет обе квитанции с действующим допуском. `NotebookKeychainPairingStore` сохраняет ключ и адрес другого устройства в том же единственном устройственном элементе Keychain, читает результат обратно и удаляет входной файл. Этот атомарный элемент также хранит отпечаток уже использованного разрешения. Прерывание между сохранением и удалением файла безопасно: повтор не создаёт другое доверие. Отзыв доступа удаляет ключ, но сохраняет использованный отпечаток; возвращённый файл не восстанавливает отозванный доступ. Другое разрешение и существующее ручное подтверждение дают отказ, а не скрытую замену.

Обнаружение в сети остаётся только обнаружением. TLS и проверка идентичностей не ослаблены. Доставка установщиком допустима по USB или по ранее сопряжённому беспроводному каналу Apple; случайное обнаружение в локальной сети такого полномочия не даёт. Проверяются точный UDID, состояние соединения и подтверждённое сопряжение Apple. Уже установленная доверенная пара подключается автоматически; ручное приглашение остаётся отдельным явным способом добавления другого устройства, а не обязательным шагом установки.

## Проверка

Core-профиль проверяет взаимные адреса, новые ключи, чужие квитанции, неверный формат и длину секрета, закрытые права и запрет перезаписи. Нативный профиль `NotebookInstallationPairingTests` проверяет Keychain до создания модели, прерванное потребление, повтор после отзыва, запрет скрытой смены ключа и блокировку модели при чужом разрешении. Полная проверка и физическое автоматическое подключение фиксируются отдельно: наличие реализации не является доказательством установки.

11 сентября: целевой Core-профиль прошёл 5 тестов; общий нативный профиль на Mac прошёл 6 тестов без ошибок и пропусков (3.108 s). Проверены также настоящий Keychain, отсутствие файла разрешения до создания модели и запрет восстановления отозванного доступа. Сборка для нативной проверки завершилась без предупреждений. Это проверка нового механизма, не полный выпуск и не доказательство связи физического iPad.

## Установленный срез 11 сентября 2026

По новому решению Амира перенос прежних данных отменён. Новое рабочее пространство активировано независимо; прежние архивы остаются в резервных копиях. Обновление автоматического сопряжения не заменяло это пространство: созданный после первой установки документ сохранён. Старый `com.amirtlinov.notebook` удалён; повторная проверка физических приложений показывает только `com.amirtlinov.notebook.preview`, Notebook Lab 0.3.15 (18).

Установленная реализация — commit `72698f8c75a90670845a93a9f0ee2b917305c6a2`. Полный `verify.sh` прошёл на неизменных 525 файлах, отпечаток `61a0bc1dac8292fe84eefd9e214bedf4189434ed320be5af722eeff63b400d1b`: 526 Core, 26 Codex, 26 проверок внешнего преобразования, 44 MCP, 119 Mac и 489 iPad runtime-тестов; оба нативных профиля без ошибок и пропусков. Проверки установщика первого Lab и сборщика пары также прошли. Полный маршрут не заменяет всю физическую приёмку рисования и ресурсов.

Проверенные подписанные приложения установлены на месте. Отпечаток реально установленного Mac совпал с подписанной сборкой. Оба приложения приняли одно и то же разрешение в Keychain и удалили входные файлы до работы модели. Доставка использовала уже сопряжённый беспроводной канал Apple CoreDevice, а не доверие к Bonjour. Действующая регистрация Codex указывает на MCP внутри установленного Mac-приложения; отдельный процесс подтвердил версию 0.3.15 и 19 инструментов.

Живой MCP подтвердил подключение физического iPad. Временное действие `C6A66C5E-8870-416C-B663-62176C6D41B5` сохранено, прочитано обратно и получило отдельные подтверждения получения и показа на iPad. После холодного перезапуска обоих приложений соединение восстановилось без нового разрешения, приглашения или ручного подтверждения; запись читалась в прежнем виде. Затем отменено только это тестовое действие. Это не обещание постоянного исполнения приложения в фоне iPadOS.

Локальные доказательства лежат в `.build/installation-pairing-full-evidence`, `.build/installation-pairing-signed` и `.build/automatic-pairing-install-20260911-1317`. Приватные разрешения не публикуются в git. Независимая копия перед обновлением: `/Users/amir/Documents/Notebook Backups/2026-09-11-1617-before-autoconnect`; прежние копии также сохранены. Профиль разработки iPad истекает 16 сентября 2026 в 14:15:33 UTC; это не постоянная магазинная подпись.
