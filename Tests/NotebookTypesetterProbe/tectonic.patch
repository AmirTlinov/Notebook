diff --git a/crates/bridge_harfbuzz/Cargo.toml b/crates/bridge_harfbuzz/Cargo.toml
index 85e8516..fa50262 100644
--- a/crates/bridge_harfbuzz/Cargo.toml
+++ b/crates/bridge_harfbuzz/Cargo.toml
@@ -31,7 +31,7 @@ cc = "^1.0.66"
 tectonic_dep_support = { path = "../dep_support", version = ">=0.1.0,<1" }
 tectonic_cfg_support = { path = "../cfg_support", version = ">=0.1.5,<1" }
 
-[target.'cfg(target_os = "macos")'.dependencies]
+[target.'cfg(any(target_os = "macos", target_os = "ios"))'.dependencies]
 tectonic_mac_core = { path = "../mac_core", version = ">=0.1.0,<1" }
 
 [features]
diff --git a/crates/bridge_harfbuzz/build.rs b/crates/bridge_harfbuzz/build.rs
index 5cbdf52..5eec059 100644
--- a/crates/bridge_harfbuzz/build.rs
+++ b/crates/bridge_harfbuzz/build.rs
@@ -42,7 +42,7 @@ mod inner {
         let mut sep = "cargo:include-path=";
 
         let is_macos_external =
-            target_cfg!(target_os = "macos") && env::var("CARGO_FEATURE_EXTERNAL_HARFBUZZ").is_ok();
+            target_cfg!(any(target_os = "macos", target_os = "ios")) && env::var("CARGO_FEATURE_EXTERNAL_HARFBUZZ").is_ok();
 
         dep.foreach_include_path(|p| {
             // HACK: On macOS, the default brew harfbuzz for some reason points into the harfbuzz
diff --git a/crates/bridge_harfbuzz/src/lib.rs b/crates/bridge_harfbuzz/src/lib.rs
index 1dc3fa3..b1fab8d 100644
--- a/crates/bridge_harfbuzz/src/lib.rs
+++ b/crates/bridge_harfbuzz/src/lib.rs
@@ -37,7 +37,7 @@ mod linkage {
     #[allow(unused_imports)]
     use tectonic_bridge_freetype2 as clippyrenamehack1;
     #[allow(unused_imports)]
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     use tectonic_mac_core as clippyrenamehack2;
 }
 
diff --git a/crates/engine_xetex/build.rs b/crates/engine_xetex/build.rs
index 9243a44..c70b6ab 100644
--- a/crates/engine_xetex/build.rs
+++ b/crates/engine_xetex/build.rs
@@ -108,7 +108,7 @@ fn main() {
 
     // Platform-specific adjustments:
 
-    let is_mac_os = target_cfg!(target_os = "macos");
+    let is_mac_os = target_cfg!(any(target_os = "macos", target_os = "ios"));
 
     if is_mac_os {
         c_cfg.define("XETEX_MAC", Some("1"));
diff --git a/crates/engine_xetex/xetex/xetex-macos.c b/crates/engine_xetex/xetex/xetex-macos.c
index b518005..48ef39c 100644
--- a/crates/engine_xetex/xetex/xetex-macos.c
+++ b/crates/engine_xetex/xetex/xetex-macos.c
@@ -41,7 +41,9 @@ authorization from the copyright holders.
 #include "teckit-c-Engine.h"
 #include "xetex-ext.h"
 
-#include <ApplicationServices/ApplicationServices.h>
+#include <CoreFoundation/CoreFoundation.h>
+#include <CoreGraphics/CoreGraphics.h>
+#include <CoreText/CoreText.h>
 
 
 static inline double
@@ -115,7 +117,7 @@ DoAATLayout(void* p, int justify)
     line = CTTypesetterCreateLine(typesetter, CFRangeMake(0, txtLen));
     if (justify) {
         CGFloat lineWidth = TeXtoPSPoints(Fix2D(node_width(node)));
-        CTLineRef justifiedLine = CTLineCreateJustifiedLine(line, TeXtoPSPoints(Fix2D(fract1)), lineWidth);
+        CTLineRef justifiedLine = CTLineCreateJustifiedLine(line, TeXtoPSPoints(Fix2D(((int32_t)0x40000000L))), lineWidth);
         // TODO(jjgod): how to handle the case when justification failed? for
         // now we just fallback to use the original line.
         if (justifiedLine) {
diff --git a/crates/engine_xetex/xetex/xetex-xetex0.c b/crates/engine_xetex/xetex/xetex-xetex0.c
index b9cade3..ffcd87c 100644
--- a/crates/engine_xetex/xetex/xetex-xetex0.c
+++ b/crates/engine_xetex/xetex/xetex-xetex0.c
@@ -5152,9 +5152,14 @@ check_outer_validity(void)
     case (s + SUB_MARK): case (s + LETTER): case (s + OTHER_CHAR)
 
 
+extern int notebook_typeset_interrupted(void);
 void
 get_next(void)
 {
+    static uint32_t notebook_poll = 0;
+    if ((++notebook_poll & 1023u) == 0 && notebook_typeset_interrupted()) {
+        fatal_error("Notebook typesetting interrupted");
+    }
     int32_t k;
     int32_t t;
     unsigned char /*max_char_code */ cat;
diff --git a/crates/engine_xetex/xetex/xetex-xetexd.h b/crates/engine_xetex/xetex/xetex-xetexd.h
index 49746c9..d05bba5 100644
--- a/crates/engine_xetex/xetex/xetex-xetexd.h
+++ b/crates/engine_xetex/xetex/xetex-xetexd.h
@@ -24,7 +24,9 @@ BEGIN_EXTERN_C
 /* include this here to avoid conflict between clang's emmintrin.h and
  * texmfmem.h. Should be removed once a fixed clang is widely available
  * http://llvm.org/bugs/show_bug.cgi?id=14964 */
-#include <ApplicationServices/ApplicationServices.h>
+#include <CoreFoundation/CoreFoundation.h>
+#include <CoreGraphics/CoreGraphics.h>
+#include <CoreText/CoreText.h>
 #endif
 
 #define odd(x) ((x) & 1)
diff --git a/crates/mac_core/build.rs b/crates/mac_core/build.rs
index 4280ae3..fed628e 100644
--- a/crates/mac_core/build.rs
+++ b/crates/mac_core/build.rs
@@ -3,13 +3,13 @@
 use tectonic_cfg_support::target_cfg;
 
 fn main() {
-    let is_mac_os = target_cfg!(target_os = "macos");
+    let is_mac_os = target_cfg!(any(target_os = "macos", target_os = "ios"));
 
     if is_mac_os {
         println!("cargo:rustc-link-lib=framework=Foundation");
         println!("cargo:rustc-link-lib=framework=CoreFoundation");
         println!("cargo:rustc-link-lib=framework=CoreGraphics");
         println!("cargo:rustc-link-lib=framework=CoreText");
-        println!("cargo:rustc-link-lib=framework=AppKit");
+        if target_cfg!(target_os = "macos") { println!("cargo:rustc-link-lib=framework=AppKit"); }
     }
 }
diff --git a/crates/mac_core/src/lib.rs b/crates/mac_core/src/lib.rs
index a47825d..e386768 100644
--- a/crates/mac_core/src/lib.rs
+++ b/crates/mac_core/src/lib.rs
@@ -3,7 +3,7 @@
 //! This currently includes only Foundation and CoreText, as those are the libraries needed by
 //! Tectonic.
 
-#![cfg(target_os = "macos")]
+#![cfg(any(target_os = "macos", target_os = "ios"))]
 
 use std::mem::ManuallyDrop;
 use std::ptr::NonNull;
diff --git a/crates/xetex_layout/Cargo.toml b/crates/xetex_layout/Cargo.toml
index d0f5f8e..b377f4d 100644
--- a/crates/xetex_layout/Cargo.toml
+++ b/crates/xetex_layout/Cargo.toml
@@ -34,10 +34,10 @@ enrede = { version = "0.2", features = ["alloc"] }
 cc = "^1.0.66"
 tectonic_cfg_support = { path = "../cfg_support", version = ">=0.1.3,<1" }
 
-[target.'cfg(not(target_os = "macos"))'.dependencies]
+[target.'cfg(not(any(target_os = "macos", target_os = "ios")))'.dependencies]
 tectonic_bridge_fontconfig = { path = "../bridge_fontconfig", version = ">=0.1.0,<1" }
 
-[target.'cfg(target_os = "macos")'.dependencies]
+[target.'cfg(any(target_os = "macos", target_os = "ios"))'.dependencies]
 tectonic_mac_core = { path = "../mac_core", version = ">=0.1.0,<1" }
 
 [features]
diff --git a/crates/xetex_layout/cbindgen.toml b/crates/xetex_layout/cbindgen.toml
index 8337ea6..a19c92a 100644
--- a/crates/xetex_layout/cbindgen.toml
+++ b/crates/xetex_layout/cbindgen.toml
@@ -5,7 +5,9 @@ include_guard = "XETEX_LAYOUT_BINDINGS_H"
 includes = ["harfbuzz/hb.h", "harfbuzz/hb-ft.h"]
 after_includes = """
 #ifdef XETEX_MAC
-#include <ApplicationServices/ApplicationServices.h>
+#include <CoreFoundation/CoreFoundation.h>
+#include <CoreGraphics/CoreGraphics.h>
+#include <CoreText/CoreText.h>
 #else
 #include <fontconfig/fontconfig.h>
 #endif
diff --git a/crates/xetex_layout/layout/tectonic_xetex_layout.h b/crates/xetex_layout/layout/tectonic_xetex_layout.h
index c60df97..036523c 100644
--- a/crates/xetex_layout/layout/tectonic_xetex_layout.h
+++ b/crates/xetex_layout/layout/tectonic_xetex_layout.h
@@ -8,7 +8,9 @@
 #include "harfbuzz/hb.h"
 #include "harfbuzz/hb-ft.h"
 #ifdef XETEX_MAC
-#include <ApplicationServices/ApplicationServices.h>
+#include <CoreFoundation/CoreFoundation.h>
+#include <CoreGraphics/CoreGraphics.h>
+#include <CoreText/CoreText.h>
 #else
 #include <fontconfig/fontconfig.h>
 #endif
diff --git a/crates/xetex_layout/src/c_api.rs b/crates/xetex_layout/src/c_api.rs
index 1e15ed4..ea5abaa 100644
--- a/crates/xetex_layout/src/c_api.rs
+++ b/crates/xetex_layout/src/c_api.rs
@@ -2,7 +2,7 @@ use crate::engine::LayoutEngine;
 use crate::font::Font;
 use std::collections::BTreeMap;
 use std::sync::Mutex;
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 use tectonic_bridge_fontconfig as fc;
 
 mod engine;
@@ -26,10 +26,10 @@ pub struct GlyphBBox {
     pub y_max: f32,
 }
 
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 pub type Fixed = i32;
 /// cbindgen:ignore
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 pub type Fixed = u32;
 pub type OTTag = u32;
 pub type GlyphID = u16;
@@ -37,15 +37,15 @@ pub type GlyphID = u16;
 pub type XeTeXFont = *mut Font;
 /// cbindgen:ignore
 pub type XeTeXLayoutEngine = *mut LayoutEngine;
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 pub(crate) type RawPlatformFontRef = *mut fc::sys::FcPattern;
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 pub(crate) type RawPlatformFontRef = tectonic_mac_core::sys::CTFontDescriptorRef;
 /// cbindgen:ignore
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 pub(crate) type PlatformFontRef = fc::Pattern;
 /// cbindgen:ignore
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 pub(crate) type PlatformFontRef = tectonic_mac_core::CTFontDescriptor;
 
 /// key is combined value representing `(font_id, glyph)`
diff --git a/crates/xetex_layout/src/c_api/font.rs b/crates/xetex_layout/src/c_api/font.rs
index ae4fb65..04ac3c8 100644
--- a/crates/xetex_layout/src/c_api/font.rs
+++ b/crates/xetex_layout/src/c_api/font.rs
@@ -5,7 +5,7 @@ use std::ffi::{CStr, CString};
 use std::ptr;
 use tectonic_bridge_freetype2 as ft;
 use tectonic_bridge_harfbuzz as hb;
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 use tectonic_mac_core::{sys::CTFontRef, CTFont, CoreType};
 
 #[no_mangle]
@@ -213,7 +213,7 @@ pub unsafe extern "C" fn countFeatures(
     rval as libc::c_uint
 }
 
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 #[no_mangle]
 pub unsafe extern "C" fn getFileNameFromCTFont(
     ct_font: CTFontRef,
diff --git a/crates/xetex_layout/src/c_api/manager.rs b/crates/xetex_layout/src/c_api/manager.rs
index b66a408..b17ec61 100644
--- a/crates/xetex_layout/src/c_api/manager.rs
+++ b/crates/xetex_layout/src/c_api/manager.rs
@@ -36,14 +36,14 @@ pub unsafe extern "C" fn findFontByName(
         Some(slice::from_raw_parts_mut(var.cast(), len))
     };
 
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     return FontManager::with_font_manager(|mgr| {
         use tectonic_mac_core::CoreType;
         mgr.find_font(name, var, size)
             .map(tectonic_mac_core::CTFontDescriptor::into_type_ref)
             .unwrap_or(ptr::null_mut())
     });
-    #[cfg(not(target_os = "macos"))]
+    #[cfg(not(any(target_os = "macos", target_os = "ios")))]
     FontManager::with_font_manager(|mgr| {
         mgr.find_font(name, var, size)
             .map(|pat| pat.as_ref().as_ptr())
diff --git a/crates/xetex_layout/src/font.rs b/crates/xetex_layout/src/font.rs
index 275c43c..480a32a 100644
--- a/crates/xetex_layout/src/font.rs
+++ b/crates/xetex_layout/src/font.rs
@@ -7,11 +7,11 @@ use std::path::Path;
 use std::str::FromStr;
 use std::sync::{Arc, Mutex, OnceLock};
 use tectonic_bridge_core::{CoreBridgeState, FileFormat};
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 use tectonic_bridge_fontconfig as fc;
 use tectonic_bridge_freetype2 as ft;
 use tectonic_bridge_harfbuzz as hb;
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 use tectonic_mac_core::{
     CFArray, CFDictionary, CFType, CFUrl, CTFont, CTFontDescriptor, FontAttribute, FontNameKey,
 };
@@ -147,7 +147,7 @@ pub(crate) fn get_larger_script_list_table_ot(font: &Font) -> hb::ot::Table<'_>
 
 enum FontKind {
     FtFont,
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     Mac(CTFontDescriptor, Option<CTFont>),
 }
 
@@ -175,7 +175,7 @@ pub struct Font {
 }
 
 impl Font {
-    #[cfg(not(target_os = "macos"))]
+    #[cfg(not(any(target_os = "macos", target_os = "ios")))]
     pub(crate) fn new(font: PlatformFontRef, point_size: f32) -> Result<Font, ()> {
         let path = font
             .as_ref()
@@ -187,7 +187,7 @@ impl Font {
         Font::new_path_index(path, index as usize, point_size)
     }
 
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     pub(crate) fn new(descriptor: PlatformFontRef, point_size: f32) -> Result<Font, ()> {
         let mut out = Font {
             units_per_em: 0,
@@ -347,7 +347,7 @@ impl Font {
         Ok(())
     }
 
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     fn initialize_mac(&mut self) -> Result<(), ()> {
         let FontKind::Mac(descriptor, font_ref) = &mut self.kind else {
             return Err(());
@@ -556,7 +556,7 @@ impl Font {
     }
 }
 
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 pub(crate) fn get_file_name_from_ct_font(ct_font: &CTFont, index: &mut u32) -> Option<CString> {
     let url = ct_font
         .attr(FontAttribute::URL)
diff --git a/crates/xetex_layout/src/manager.rs b/crates/xetex_layout/src/manager.rs
index 5f89dfe..698bc98 100644
--- a/crates/xetex_layout/src/manager.rs
+++ b/crates/xetex_layout/src/manager.rs
@@ -10,9 +10,9 @@ use std::convert::TryFrom;
 use std::ffi::{CStr, CString};
 use tectonic_bridge_freetype2 as ft;
 
-#[cfg(not(target_os = "macos"))]
+#[cfg(not(any(target_os = "macos", target_os = "ios")))]
 mod fc;
-#[cfg(target_os = "macos")]
+#[cfg(any(target_os = "macos", target_os = "ios"))]
 mod mac;
 
 thread_local! {
@@ -313,11 +313,11 @@ impl FontManager {
     fn init_font_manager() {
         let backend: Box<dyn FontManagerBackend>;
 
-        #[cfg(target_os = "macos")]
+        #[cfg(any(target_os = "macos", target_os = "ios"))]
         {
             backend = Box::new(mac::MacBackend::new());
         }
-        #[cfg(not(target_os = "macos"))]
+        #[cfg(not(any(target_os = "macos", target_os = "ios")))]
         {
             backend = Box::new(fc::FcBackend::new());
         }
@@ -778,7 +778,7 @@ impl FontManager {
         }
     }
 
-    #[cfg_attr(target_os = "macos", allow(unused))]
+    #[cfg_attr(any(target_os = "macos", target_os = "ios"), allow(unused))]
     fn prepend_to_list<T: Into<CString> + AsRef<CStr>>(list: &mut Vec<CString>, str: T) {
         *list = list.drain(..).filter(|s| **s != *str.as_ref()).collect();
         list.insert(0, str.into());
diff --git a/crates/xetex_layout/src/utils.rs b/crates/xetex_layout/src/utils.rs
index ee7efc8..c03a42e 100644
--- a/crates/xetex_layout/src/utils.rs
+++ b/crates/xetex_layout/src/utils.rs
@@ -10,13 +10,13 @@ pub fn d_to_fix(d: f64) -> Fixed {
 }
 
 pub fn raw_to_rs(font: RawPlatformFontRef) -> Option<PlatformFontRef> {
-    #[cfg(target_os = "macos")]
+    #[cfg(any(target_os = "macos", target_os = "ios"))]
     let out = {
         use tectonic_mac_core::CoreType;
         // SAFETY: Pointer must be from us, and is thus a borrowed ref
         NonNull::new(font.cast_mut()).map(|ptr| unsafe { PlatformFontRef::new_borrowed(ptr) })
     };
-    #[cfg(not(target_os = "macos"))]
+    #[cfg(not(any(target_os = "macos", target_os = "ios")))]
     // SAFETY: Pointer must be from us, and is thus a borrowed ref
     let out = { unsafe { NonNull::new(font).map(|p| PlatformFontRef::from_raw_borrowed(p)) } };
     out
diff --git a/src/driver.rs b/src/driver.rs
index 5050fd0..3a20911 100644
--- a/src/driver.rs
+++ b/src/driver.rs
@@ -227,7 +227,7 @@ struct BridgeState {
     mem: MemoryIo,
 
     /// The main filesystem backing for input files in the project.
-    filesystem: FilesystemIo,
+    filesystem: Box<dyn IoProvider>,
 
     /// Extra paths we search through for files.
     extra_search_paths: Vec<FilesystemIo>,
@@ -802,6 +802,7 @@ pub struct ProcessingSessionBuilder {
     tex_input_name: Option<String>,
     output_dest: OutputDestination,
     filesystem_root: Option<PathBuf>,
+    input_provider: Option<Box<dyn IoProvider>>,
     format_name: Option<String>,
     format_cache_path: Option<PathBuf>,
     output_format: OutputFormat,
@@ -832,6 +833,12 @@ impl ProcessingSessionBuilder {
         }
     }
 
+    /// Replace project filesystem access with an explicit input capability.
+    pub fn input_provider(&mut self, provider: Box<dyn IoProvider>) -> &mut Self {
+        self.input_provider = Some(provider);
+        self
+    }
+
     /// Sets the path to the primary input file.
     ///
     /// If a primary input path is not specified, we will default to reading it from stdin.
@@ -1184,7 +1191,8 @@ impl ProcessingSessionBuilder {
             Vec::new()
         };
 
-        let filesystem = FilesystemIo::new(&filesystem_root, false, true, hidden_input_paths);
+        let filesystem = self.input_provider.unwrap_or_else(||
+            Box::new(FilesystemIo::new(&filesystem_root, false, true, hidden_input_paths)));
 
         let mem = MemoryIo::new(true);
 
